# Docstring 80% Housekeeping Implementation Plan

> **For workers:** Execute task-by-task. Review gate: plan review before implementation, oracle review after impl.

**Goal:** Raise docstring coverage to >=80% in every module under `src/muhideen/` without behavior change.

**Architecture:** Docs-only change across `core/`, `api/`, `adapters/`, `domain/`, `engine/`, plus package `__init__` files and the two console entrypoints. No logic, no signatures, no contract, no schema, no dependency changes.

**Tech Stack:** Python 3.11+, uv, ruff, strict pyright, import-linter, pytest (coverage gate 95%).

**References:** `pyproject.toml:52-65` (coverage config), `ARCHITECTURE.md` (layer ownership), `CONTRIBUTING.md:38-51` (quality gate), style precedent `src/muhideen/adapters/calc_mabims.py:39-52` (one-line imperative helpers) and `src/muhideen/domain/hijri.py:10-14`, measured baseline `src/muhideen/core/ports.py:17-89`, `src/muhideen/api/app.py:82-447`, `src/muhideen/adapters/sqlite_repo.py:48-362`.

**Branch:** `pre-phase-1b-housekeeping` (current branch; commit here, do not push).

---

## File Structure

- Modify: `src/muhideen/core/ports.py` — 10 Protocol classes + 13 port methods gain one-line docstrings.
- Modify: `src/muhideen/core/values.py` — `PrayerState`, `ScheduleSource`, `IqamahRule`, `Settings` gain class docstrings; `src/muhideen/core/errors.py` — 2 `__init__` methods gain docstrings.
- Modify: `src/muhideen/api/app.py` — module helpers, lifespan, middleware, error handlers, and endpoint handlers gain docstrings; `src/muhideen/api/auth.py` — `SessionStore`/`RateLimiter` methods + `_purge_locked`/`_check` gain docstrings; `src/muhideen/api/dto.py` — `PrayerDayDTO`, `NextEventDTO`, validators, and all `from_domain`/`to_domain` mappers gain docstrings.
- Modify: `src/muhideen/adapters/sqlite_repo.py`, `src/muhideen/adapters/sse_bus.py`, `src/muhideen/adapters/system_clock.py`, `src/muhideen/adapters/time_sync.py`, `src/muhideen/adapters/migrate.py` — repo methods, bus accessors, clock accessors, probe helpers gain docstrings.
- Modify: `src/muhideen/domain/fallback.py` (`FallbackResult`), `src/muhideen/domain/prayer_state.py` (`_adhan_dt`, `_event`), `src/muhideen/engine/engine.py` (`__init__`, `_resolve_day`, `_tomorrow`), `src/muhideen/seed.py` (`_parser`), `src/muhideen/service.py` (`_parser`).
- Modify: `src/muhideen/__init__.py`, `src/muhideen/adapters/__init__.py`, `src/muhideen/views/__init__.py` — add one-line package docstrings.
- Test: docstring sweep script (AST counter over `src/muhideen/`) — red before (<80% in 19 files), green after.

No `views/` logic change, no `migrations/` change, no contract/fixture change.

---

### Task 1: Core ports + values + errors

**Files:** `src/muhideen/core/ports.py`, `src/muhideen/core/values.py`, `src/muhideen/core/errors.py`

- [ ] Run the AST sweep (counter over modules+classes+functions) and record red: `core/ports.py` ~21%, `core/values.py` ~55%, `core/errors.py` ~78%.
- [ ] Add one-line docstrings: every Protocol class states the capability it abstracts; every port method states its contract (`None`-means, error-means); `PrayerState`/`ScheduleSource` state wire meaning; `IqamahRule`/`Settings` state the invariant; both `__init__` methods state carried context.
- [ ] Verify: sweep shows all three files >=80%; `uv run ruff check . && uv run ruff format --check . && uv run pyright` clean; `uv run pytest -m "unit or contract" -q` passes.

### Task 2: API surface (app + auth + dto)

**Files:** `src/muhideen/api/app.py`, `src/muhideen/api/auth.py`, `src/muhideen/api/dto.py`

- [ ] Record red: `api/app.py` ~36%, `api/auth.py` ~39%, `api/dto.py` ~58% per the sweep.
- [ ] Add one-line docstrings: `_require_tz_aware` (reject rule), `_frame` (SSE wire shape), `_run_ticker` (cadence + swallow rule), `_lifespan` (migrate + background + flush), `_docs_gate` (LAN+session gate), error handlers (status mapping), each endpoint (route + success/error shape), `SessionStore`/`RateLimiter` methods (TTL, purge, window semantics), `_check` (401 rule), DTO validators/mappers (direction + narrowing).
- [ ] Verify: sweep shows all three files >=80%; ruff + format + pyright clean; `uv run pytest -m "contract or e2e" -q` passes.

### Task 3: Adapters (sqlite, sse, clock, sync, migrate)

**Files:** `src/muhideen/adapters/sqlite_repo.py`, `src/muhideen/adapters/sse_bus.py`, `src/muhideen/adapters/system_clock.py`, `src/muhideen/adapters/time_sync.py`, `src/muhideen/adapters/migrate.py`

- [ ] Record red: `sqlite_repo.py` ~40%, `sse_bus.py` ~29%, `system_clock.py` ~40%, `time_sync.py` ~57%, `migrate.py` ~71%.
- [ ] Add one-line docstrings: repo constructors (dependency held), `get_day`/`save_day`/`last_known` (lookup/upsert semantics), `load`/`save` (boundary + error translation), `record_seen`/`flush`/`_flush_locked` (buffering + atomicity), `has_users`/`create_user`/`verify` (uniqueness + hash failure rule), bus `__init__`/property/`subscribe`/`unsubscribe`/`publish` (fan-out semantics), clock accessors (tz/monotonic source), `_read`/`_chrony_synced` (fallback + fail-closed rule), `_up_files`/`_version_of` (ordering/version parsing).
- [ ] Verify: sweep shows all five files >=80%; ruff + format + pyright clean; `uv run pytest -m integration -q` passes.

### Task 4: Domain + engine + entrypoints + packages

**Files:** `src/muhideen/domain/fallback.py`, `src/muhideen/domain/prayer_state.py`, `src/muhideen/engine/engine.py`, `src/muhideen/seed.py`, `src/muhideen/service.py`, `src/muhideen/__init__.py`, `src/muhideen/adapters/__init__.py`, `src/muhideen/views/__init__.py`

- [ ] Record red: `fallback.py` 75%, `prayer_state.py` 60%, `engine.py` 70%, `seed.py`/`service.py` 67%, three `__init__` files 0%.
- [ ] Add docstrings: `FallbackResult` (resolved day + staleness flag), `_adhan_dt` (combine rule), `_event` (NextEvent assembly), `Engine.__init__` (injected deps held), `_resolve_day`/`_tomorrow` (lazy chain / no-last-known-for-tomorrow rule), both `_parser` functions (CLI surface), each `__init__` (package role in one line).
- [ ] Verify: sweep shows all eight files >=80%; ruff + format + pyright clean; `uv run pytest -q` full suite passes with coverage >=95%.

### Task 5: Exit gate + commit (no push)

**Files:** this plan (amend counts if execution drifted)

- [ ] Run the full gate: `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run lint-imports && uv run pytest -q` plus the AST sweep proving every module >=80%.
- [ ] Commit all changes on `pre-phase-1b-housekeeping` with message `docs: raise docstring coverage to 80pct per module`; verify `git status --short` clean and `git log -1 --stat` shows the commit; do not push.

---

## Detailed-Plan Checklist

1. **PRD refs:** none — docs-only, no requirement change.
2. **Contract diff:** none — no wire, DTO, fixture, or shipped-doc change.
3. **Changes by layer:** `core/` class+method docstrings only; `api/` handler/helper docstrings only; `adapters/` method docstrings only; `domain/`/`engine/` helper docstrings only; entrypoints `seed.py`/`service.py` parser docstrings; package `__init__` module lines. No imports added, no signatures changed.
4. **Tests by layer:** no test files added or changed; existing suite (421 tests) must stay green; the AST sweep is the task-level red/green signal.
5. **Purity / import-linter impact:** none — comment-only edits, no new imports, layer contract unchanged.
6. **Docs touched:** this plan file only; no shipped docs.
7. **Rollback:** single docs-only commit revert; no schema, no data, no contract impact.
8. **Exit gate:** AST sweep every module >=80%; `ruff check`, `ruff format --check`, `pyright`, `lint-imports`, `pytest -q` (coverage >=95%) all green; commit present locally, nothing pushed.
