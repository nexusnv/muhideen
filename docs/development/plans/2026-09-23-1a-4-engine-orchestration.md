# Slice 1A-4 Engine Orchestration Implementation Plan

> **For workers:** Execute task-by-task via `tdd` + `verification-before-completion`. Review gate: `muhideen-momus-review` (plan), `muhideen-oracle-review` (after impl).

**Goal:** Land `src/muhideen/engine/` — resolve day (FR-1.2 fallback chain over ports) → compute next event (domain §8 machine) → fan out `state`/`tick` on the `EventBus` — proven by integration tests on in-memory fakes and property tests for state invariants, unblocking frontend state-transition QA (1B-2).

**Architecture:** `engine/` layer (new code) plus two small core changes it cannot operate without: `Settings` gains `iqamah_rules` (FR-1.4) and calc config `lat`/`lon`/`method` (PRD §6.2 settings keys) so `SettingsRepo.load()` reloads them per tick (admin edits apply without restart, FR-6.1); `PrayerRepo` gains `last_known(day, zone)` so the engine can supply FR-1.2 step 3 to `domain.resolve_day` (no adapter implements the port yet, so the surface change is free now). Engine imports `core` + `domain` only: no HTTP, no SQL, no wall-clock reads (time comes from the injected `Clock`; `now` is also an explicit parameter on `resolve_day`/`next_event` for determinism). NOT changed: `domain/` (pure logic landed 1A-2), `api/` (501 stubs stay until 1A-7), `adapters/` (SQLite/JAKIM/calc/SystemClock are 1A-5/1A-6/1A-7), fixtures/contract, `views/`, `migrations/`.

**Tech Stack:** Python 3.11+, uv, ruff, strict pyright, import-linter, pytest (`unit`/`integration`/`property` markers; `contract` must stay green untouched), Hypothesis (`ci` profile from `tests/conftest.py:2-11`).

**References:** `docs/development/PHASES_AND_SLICES.md:39` slice 1A-4 row (entry 1A-3, exit integration + property), `:40` 1A-5 entry depends on this slice, `:50` 1B-2 depends on 1A-4 semantics, `ARCHITECTURE.md:39-40` Engine owns "load settings, resolve day via fallback chain, compute next event, fan out SSE" + registry-free composition, `:10-11` determinism scoped to clock + snapshot, `:61` layer contract (`api → engine → adapters → domain → core`), `:62` purity scans, `:63` coverage 95 + pinned-time test per transition, `PRD.md:78` FR-1.2 fallback order, `:80` FR-1.4 iqamah defaults (Subuh 15, Dhuhr/Asr/Maghrib 10, Isha 15, Jumuah own rule, Syuruq N/A), `:82` FR-1.6 countdowns monotonic, `:90-93` FR-2.3/2.4/2.6 state machine, `:109` FR-4.3 SSE push + 60s poll, `:163-166` §4.3 ports + in-memory-repo use-case tests, `:206` stale banner (48h or calc fallback), `:243-249` §6.2 settings keys (`zone_code|lat,lon,method`, `hijri_offset`, durations), `:304-312` §6.3 contract surfaces, `:335-345` §8 state machine + edge rules, `TESTING_STRATEGY.md:11` integration = engine + fakes, `:12` property invariants (no-overlap, monotonic targets, Syuruq never dims, midnight, re-render idempotence), `:17-20` pinned `FakeClock`, file-local doubles, Hypothesis `ci` profile, `:39` determinism is the most important test, `:43` provenance of schedules, `docs/api-contract.md:21-37` next-event shape, `:39-41` SSE event names `state`/`tick`/`config-update`, `api/fixtures/next-event.json:1` IQAMAH_COUNTDOWN vector, `api/fixtures/prayer-day.json:1` day vector, `api/fixtures/events-stream.txt:1-14` scenario order, `src/muhideen/core/values.py:42-53` `PrayerDay`, `:56-64` `NextEvent`, `:67-72` `IqamahRule`, `:75-86` `Settings` + `__post_init__` guard precedent, `src/muhideen/core/ports.py:15-20` `PrayerRepo`, `:34-38` `CalcEngine.compute_day(day, lat, lon, method)`, `:41-51` `Clock`/`EventBus.publish(event: str)`, `src/muhideen/core/errors.py:12-13` `ConfigError`, `:16-23` `ScheduleError(zone, date)`, `src/muhideen/core/__init__.py:19-49` value exports, `src/muhideen/domain/fallback.py:14-17` `FallbackResult`, `:20-24` `is_stale`, `:27-50` `resolve_day(requested, zone, now, cached, calculated, last_known)`, `src/muhideen/domain/prayer_state.py:25-42` `resolve_next_event(now, today, tomorrow, rules, settings, stale)`, `src/muhideen/domain/iqamah.py:11-26` rules lookup + `ConfigError`, `tests/unit/test_core_values.py:113-130` Settings equality/guard tests to extend, `tests/unit/test_core_ports.py:20-31` `PrayerRepo` stubs + `:110-142` isinstance parametrize lists, `tests/unit/test_domain_determinism.py:99-147` fixture-vector replay precedent, `pyproject.toml:34-42` markers, `:44-56` coverage `fail_under=95`, `:76-88` import-linter layers, `.github/workflows/ci.yml:18-24` full gate, `CONTRIBUTING.md:44-45` gate commands, `CHANGELOG.md:7-27` Unreleased entries, `docs/adr/0003-sqlite-behind-ports.md:10` in-memory fakes for tests, `CONTEXT.md:42-43` Calculation Method, `:50-51` Fallback Chain, `:53-54` Stale, precedent plans `2026-09-22-1a-2-domain-pure-logic.md`, `2026-09-22-1a-3-executable-contract.md`.

**Branch:** `feature/1a-4-engine-orchestration` (from `main` @ `5f4369d`, already created)

**Slice:** 1A-4 Engine orchestration. Entry: 1A-3 landed (`5f4369d`); baseline verified on this branch: `uv run pytest -q` = 103 passed (unit 73, contract 30), coverage 97.24%. Exit: `integration` green with fake repos; `property` green for no-overlap states + monotonic targets (plus Syuruq/midnight/idempotence per `TESTING_STRATEGY.md:12`); full `uv` gate green; `contract` suite untouched and still green.

---

## File Structure

- Create: `src/muhideen/engine/engine.py` — `STATE_EVENT = "state"`, `TICK_EVENT = "tick"`, class `Engine` with `__init__(*, settings_repo, prayer_repo, clock, event_bus, calc: CalcEngine | None = None)`, public `resolve_day(requested, zone, now) -> FallbackResult`, `next_event(now) -> NextEvent`, `tick() -> NextEvent`, private `_resolve_day(requested, zone, now, settings)`, `_calc_day(day, zone, settings)`, `_tomorrow(day, zone, settings)`, fan-out state `_last_fingerprint` / `_last_minute` (both explicitly annotated `... | None = None` — pyright strict infers `None` from an unannotated `__init__` assignment and would reject the later tuple assignment)
- Modify: `src/muhideen/engine/__init__.py` — re-export `Engine`, `STATE_EVENT`, `TICK_EVENT`
- Modify: `src/muhideen/core/values.py` — module `DEFAULT_IQAMAH_RULES: tuple[IqamahRule, ...]` (six FR-1.4 rules, Syuruq excluded) after `IqamahRule`; `Settings` gains `iqamah_rules: tuple[IqamahRule, ...] = DEFAULT_IQAMAH_RULES`, `lat: float | None = None`, `lon: float | None = None`, `method: str = "MABIMS"`; `__post_init__` gains coordinate guards (set-together + ranges) beside the existing Hijri guard; module docstring updated to name all construction guards
- Modify: `src/muhideen/core/ports.py` — `PrayerRepo` gains `last_known(self, day: date, zone: str) -> PrayerDay | None` with a docstring pinning semantics: most recent saved day with `date <= day` for `zone`, else `None`
- Modify: `src/muhideen/core/__init__.py` — import + `__all__` entry for `DEFAULT_IQAMAH_RULES`
- Modify: `tests/unit/test_core_values.py` — default-rules, calc-config-default, coordinate-guard, frozen/hash/equality-with-new-fields tests
- Modify: `tests/unit/test_core_ports.py` — `FullPrayerRepo` gains `last_known`; new `MissingLastKnownRepo` stub added to the negative isinstance list
- Create: `tests/integration/test_engine.py` — file-local fakes (`FakeSettingsRepo`, `FakePrayerRepo`, `FakeCalc`, `FakeClock`, `RecordingBus`) + 24 collected tests across Tasks 2–4 (9 + 9 + 6, parametrised variants included); module `pytestmark = pytest.mark.integration`
- Create: `tests/property/test_state_machine_properties.py` — 5 Hypothesis properties; module `pytestmark = pytest.mark.property`
- Modify: `CHANGELOG.md` — `[Unreleased]` Added/Changed entries for this slice

No `src/muhideen/api/`, `src/muhideen/domain/`, `src/muhideen/adapters/`, `src/muhideen/views/`, `migrations/`, `tools/`, `api/fixtures/`, or `docs/api-contract.md` changes (contract diff: `none` — checklist item 2).

---

### Task 1: Core vocabulary for orchestration — `Settings` aggregate + `last_known` port

**Files:** `tests/unit/test_core_values.py`, `tests/unit/test_core_ports.py` (modify), `src/muhideen/core/values.py`, `src/muhideen/core/ports.py`, `src/muhideen/core/__init__.py` (modify)

- [ ] Add failing tests. In `tests/unit/test_core_values.py`: `test_settings_default_iqamah_rules_match_prd_fr_1_4` — `Settings(masjid_name="M", zone="SGR01", hijri_offset=0).iqamah_rules` yields `{fajr:15, dhuhr:10, asr:10, maghrib:10, isha:15, jumuah:10}` keyed by `PrayerName`, every `mode == "delay"`, no `SYURUQ` key; assert `from muhideen.core import DEFAULT_IQAMAH_RULES` equals `Settings(...).iqamah_rules` (exercises the `__init__` export); `test_settings_calc_config_defaults_disabled` — `lat is None`, `lon is None`, `method == "MABIMS"`; `test_settings_rejects_half_configured_coordinates` — parametrize `[(3.1, None), (None, 101.6)]` → `pytest.raises(ValueError, match="lat and lon must be set together")`; `test_settings_rejects_out_of_range_coordinates` — `lat=91.0` and `lon=181.0` → `ValueError`; `test_settings_new_fields_frozen_hashable_and_compared` — `FrozenInstanceError` on `settings.method = "MWL"`, `hash(settings)` succeeds (rules tuple of frozen `IqamahRule` keeps `Settings` hashable), and two Settings differing only in `method` compare unequal. Existing guards (`:124-130`) must keep passing unchanged (defaults keep every existing construction site valid). Run: `uv run pytest tests/unit/test_core_values.py -v` → Expected: FAIL (`TypeError` unexpected kwarg / missing attribute). In `tests/unit/test_core_ports.py`: add `MissingLastKnownRepo` (has `get_day` + `save_day` only) to the `test_incomplete_stub_fails_isinstance` parametrize list → FAIL because `PrayerRepo` does not require `last_known` yet. Run: `uv run pytest tests/unit/test_core_ports.py -v` → Expected: FAIL.
- [ ] Implement in `src/muhideen/core/values.py`: `DEFAULT_IQAMAH_RULES` module constant (six rules per `PRD.md:80`, keyword args, after the `IqamahRule` class so the name exists before `Settings`); extend `Settings` with the four defaulted fields listed in File Structure; extend `__post_init__` with `if (self.lat is None) != (self.lon is None): raise ValueError("lat and lon must be set together")` then range guards `not -90 <= lat <= 90` / `not -180 <= lon <= 180` raising `ValueError("latitude out of range: ...")` / `ValueError("longitude out of range: ...")` (construction invariants raise `ValueError`, translation to `ConfigError` at adapters stays 1A-5 per the `values.py:1-8` docstring, which this task updates to say "Settings construction guards" instead of only the Hijri guard). In `ports.py` add `PrayerRepo.last_known` with the docstring above. Export `DEFAULT_IQAMAH_RULES` from `src/muhideen/core/__init__.py`. Verify: both pytest commands → PASS; `uv run pytest -m unit -q` → PASS (73 baseline + 6 new).

**Depends on:** 1A-1 `Settings`/`IqamahRule` shapes (`values.py:67-86`), 1A-3 landed.

### Task 2: `Engine.resolve_day` — fallback chain over ports

**Files:** `tests/integration/test_engine.py` (create), `src/muhideen/engine/engine.py`, `src/muhideen/engine/__init__.py` (create/modify)

- [ ] Create `tests/integration/test_engine.py` with `pytestmark = pytest.mark.integration`, tz `ZoneInfo("Asia/Kuala_Lumpur")`, and file-local fakes only (`TESTING_STRATEGY.md:18`): `FakeSettingsRepo(settings)` (mutable `.settings`, `.load_count` counter, `load()`/`save()`), `FakePrayerRepo(rows: dict[tuple[date, str], PrayerDay])` (`get_day`/`save_day` keyed by `(date, zone)`, `last_known(day, zone)` returns the max-date row with `row.date <= day` and matching zone else `None`, `.last_known_calls` log), `FakeCalc` (`compute_day(day, lat, lon, method)` appends to `.calls`, returns a `PrayerDay(source=CALC, zone=self.returned_zone, ...)` built from drawn base times, or raises `self.error` when set), `FakeClock(now)` (`now()`/`monotonic()`), `RecordingBus` (`.events: list[str]`, `publish` appends). Add failing tests: `test_cache_hit_fresh_skips_calc_and_last_known` — seed a JAKIM row fetched 1h ago for `(2025-10-20, "SGR01")`, `engine.resolve_day(date(2025,10,20), "SGR01", now)` → `FallbackResult(day == seed, stale is False)`, `FakeCalc.calls == []`, `repo.last_known_calls == []` (lazy: no calc/last-known query on the normal path); `test_cache_miss_uses_calc_and_normalises_zone` — parametrize `returned_zone` over `["SGR01", "OTHER"]` → result zone is `"SGR01"` either way (engine stamps `zone` via `dataclasses.replace` because `CalcEngine.compute_day` — `ports.py:34-38` — has no zone parameter but `domain/fallback.py:36-43` requires zone match), `source is CALC`, `stale is True` (calc fallback shows the banner, `PRD.md:206`), `calc.calls == [(date, 3.1, 101.6, "MABIMS")]` from settings, `last_known_calls == []`; `test_cache_miss_calc_disabled_falls_to_last_known` — `calc=None`, seed only an older row → that row returned, `stale is True` (forced by `domain/fallback.py:44-45`); `test_settings_without_coordinates_disable_calc` — calc port present but `lat/lon` are `None` → `calc.calls == []`, last-known path used; `test_calc_failure_falls_to_last_known` — parametrize `error` over `[ScheduleError("boom"), ValueError("bad ephemeris")]` → last-known result, exception not propagated (engine treats `MuhideenError`/`ValueError` from calc as a miss); `test_all_sources_miss_raises_schedule_error` — empty repo, calc disabled → `pytest.raises(ScheduleError)` with `.zone == "SGR01"` and `.date == "2025-10-20"`; `test_resolve_day_loads_settings_once` — `load_count == 1`. Run: `uv run pytest tests/integration/test_engine.py -v` → Expected: FAIL (`ModuleNotFoundError`, no `muhideen.engine.engine`).
- [ ] Implement `Engine` in `src/muhideen/engine/engine.py` per File Structure: `resolve_day(requested, zone, now)` loads settings once and delegates to `_resolve_day`, which runs `cached = prayer_repo.get_day(requested, zone)`; `calculated = self._calc_day(requested, zone, settings)` only when `cached is None`; `last_known = prayer_repo.last_known(requested, zone)` only when both are `None`; then returns `muhideen.domain.resolve_day(...)`'s `FallbackResult`. `_calc_day` returns `None` when `self._calc is None` or `settings.lat is None` or `settings.lon is None`, else calls `compute_day(day, settings.lat, settings.lon, settings.method)` inside `try/except (MuhideenError, ValueError): return None`, then normalises `computed.zone` to the requested zone. Imports: `dataclasses.replace`, `datetime.date/datetime/timedelta`, `muhideen.core.{errors,ports,values}`, `muhideen.domain` — no fastapi/sqlite3/httpx, no `datetime.now`. Re-export `Engine`, `STATE_EVENT`, `TICK_EVENT` from `src/muhideen/engine/__init__.py`. Verify: same pytest command → PASS (9 collected); `uv run pytest -m integration -q` → PASS; `uv run pytest -q` → PASS (contract untouched).

**Depends on:** Task 1 (`Settings.lat/lon/method`, `PrayerRepo.last_known`), 1A-2 `domain.resolve_day`.

### Task 3: `Engine.next_event` — settings → day → §8 event

**Files:** `tests/integration/test_engine.py` (extend), `src/muhideen/engine/engine.py` (extend)

- [ ] Add failing tests: `test_engine_replays_contract_fixture_vector` — seed `FakeSettingsRepo` with `Settings(masjid_name="Masjid Test", zone="SGR01", hijri_offset=0, iqamah_rules=DEFAULT_IQAMAH_RULES)` overridden so the DHUHR rule is `delay_minutes=15` (fixture implies 12:15→12:30, `tests/unit/test_domain_determinism.py:100-134` precedent); seed `FakePrayerRepo` from `api/fixtures/prayer-day.json` (times, zone, `source=JAKIM`, `fetched_at=now−11h`); `now = datetime.fromisoformat(json.loads(api/fixtures/next-event.json)["now"])` → `NextEventDTO.from_domain(engine.next_event(now)).model_dump(mode="json") == json.loads(next-event.json)` — engine output round-trips the contract fixture byte-for-byte through the 1A-3 DTO; `test_settings_reloaded_every_call_without_restart` — `next_event(now)`, mutate `.settings.adhan_duration_s` 180→300 (or DHUHR delay 15→10), `next_event(now)` again reflects it, `load_count == 2` (no engine-level settings cache — admin edits land live, FR-6.1); `test_settings_loaded_once_per_next_event` — fresh engine, one call → `load_count == 1` despite today+tomorrow resolution; `test_midnight_uses_tomorrow_row` — `now` at 20:30 after Isha dim, rows for today and tomorrow → `state is NORMAL`, `next_prayer is FAJR`, `adhan_at == datetime.combine(now.date()+1d, tomorrow.fajr, tz)`, iqamah/dim from tomorrow's Fajr; `test_midnight_tomorrow_from_calc` — no tomorrow row, calc enabled → calc day's Fajr used (covers `_tomorrow` calc branch); `test_midnight_without_tomorrow_uses_today_fajr` — no tomorrow row, calc disabled → `adhan_at == now.date()+1d + today.fajr` time (domain carry rule); `test_stale_day_passes_into_event` — only a last-known row available (72h old) → `event.stale is True`; `test_missing_iqamah_rule_raises_config_error` — `Settings(..., iqamah_rules=())` → `pytest.raises(ConfigError)` propagates from `domain/iqamah.py:21`; `test_schedule_error_propagates` — empty repo → `pytest.raises(ScheduleError)` (API maps to 5xx in 1A-7). Run: `uv run pytest tests/integration/test_engine.py -v` → Expected: FAIL (Task 2's 9 tests pass; the new ones raise `AttributeError`, no `next_event`).
- [ ] Implement `next_event(now: datetime) -> NextEvent` in `engine.py`: `settings = self._settings_repo.load()` once; `zone = settings.zone` (installation has exactly one zone, `CONTEXT.md:39-40`); `today = self._resolve_day(now.date(), zone, now, settings)`; `tomorrow = self._tomorrow(now.date() + timedelta(days=1), zone, settings)`; `rules = {rule.prayer: rule for rule in settings.iqamah_rules}`; return `domain.resolve_next_event(now, today.day, tomorrow, rules, settings, today.stale)` (`stale` provenance flows from the day resolution, `TESTING_STRATEGY.md:43`). `_tomorrow` returns the cached row when present, else `_calc_day(...)`, else `None`, with a comment that `last_known` is deliberately never used for tomorrow (a past template day must not seed tomorrow's Fajr). Verify: same pytest command → PASS (18 collected); `uv run pytest -m integration -q` → PASS; `uv run pytest -q` → PASS.

**Depends on:** Task 2 (`_resolve_day`, `_calc_day`).

### Task 4: `Engine.tick` — Clock-driven fan-out

**Files:** `tests/integration/test_engine.py` (extend), `src/muhideen/engine/engine.py` (extend)

- [ ] Add failing tests (all with a seeded fresh JAKIM day for `2025-10-20` SGR01, Dhuhr 12:15, default rules/settings, fresh `FakeClock` + fresh `RecordingBus`): `test_first_tick_publishes_state_then_tick` — clock pinned `2025-10-20 12:09+08`, `engine.tick()` → `bus.events == ["state", "tick"]` (literal strings, pinned to `docs/api-contract.md:41` — not the module constants, so the test cannot go tautological), returned `event.now == clock.now()`, `event.state is NORMAL`; `test_repeat_tick_same_minute_silent` — tick twice at the same pinned instant → second call appends nothing (`bus.events` stays `["state", "tick"]`); `test_minute_rollover_publishes_tick_only` — first tick at 12:12 (`PRE_ADHAN`, already entered at 12:10), `bus.events.clear()`, advance the clock to 12:13 with state unchanged → appends `["tick"]` only; `test_state_transition_publishes_state_before_tick` — tick at 12:09 (NORMAL), `bus.events.clear()`, advance to 12:10 (`PRE_ADHAN` starts exactly `adhan_at − PRE_ADHAN_WINDOW`, `prayer_state.py:18`) → `bus.events == ["state", "tick"]` (state first — order is part of this slice's pinned behaviour); `test_target_change_same_minute_publishes_state_only` — tick at 12:14 (PRE_ADHAN, IQAMAH target 12:25), clear bus, mutate `settings_repo.settings` so the DHUHR rule delay is 5 (target 12:20), tick again at the same clock minute → `bus.events == ["state"]` (fingerprint includes targets + `stale`, minute unchanged — proves admin edits fan out without restart and without a spurious `tick`); `test_engine_never_publishes_config_update` — across all of the above scenarios `RecordingBus.events` contains only `{"state", "tick"}` and never `"config-update"` (that publish belongs to the admin save path in 1A-7). Run: `uv run pytest tests/integration/test_engine.py -v` → Expected: FAIL (Tasks 2–3's 18 tests pass; the new ones raise `AttributeError`, no `tick`).
- [ ] Implement `tick() -> NextEvent` in `engine.py`: `now = self._clock.now()` (the only time read; `Clock` port, `ports.py:41-45`); `event = self.next_event(now)`; `fingerprint = (event.state, event.next_prayer, event.adhan_at, event.iqamah_at, event.dim_until, event.stale)` (deliberately excludes `event.now`, which changes every call); `minute = (now.year, now.month, now.day, now.hour, now.minute)`; `if fingerprint != self._last_fingerprint: publish(STATE_EVENT); self._last_fingerprint = fingerprint`; `if minute != self._last_minute: publish(TICK_EVENT); self._last_minute = minute`; return `event`. Both instance fields start `None` so the first tick publishes `["state", "tick"]` uniformly (no special-case branch). A backwards clock step only yields one extra `tick` heartbeat — no handling needed (countdown monotonicity is client-side, `PRD.md:82`). Verify: same pytest command → PASS (24 collected); `uv run pytest -m integration -q` → PASS; `uv run pytest -q` → PASS.

**Depends on:** Task 3 (`next_event`), Task 1 (`Clock`/`EventBus` ports unchanged).

### Task 5: Property invariants — no-overlap states, monotonic targets

**Files:** `tests/property/test_state_machine_properties.py` (create)

- [ ] Create the file with `pytestmark = pytest.mark.property`, `from hypothesis import given, strategies as st`, tz-aware pinned datetimes (`Asia/Kuala_Lumpur`). Strategies: `DATES = st.sampled_from([date(2025, 10, 22), date(2025, 10, 24)])` (Wednesday + Friday so the Jumuah relabel runs under property pressure); `@st.composite def schedules(draw, max_minute=1439)` producing 6 strictly increasing minute-of-day ints — first drawn in `[0, max_minute - 5*80]`, then each subsequent gap drawn in `[80, max_minute - prev - 80*slots_left_after]` (keeps every gap ≥ 80 minutes and the last slot ≤ `max_minute`; 80 min covers the worst-case default window span 5 min PRE_ADHAN + ≤5 min adhan (`adhan_duration_s ≤ 300` below) + ≤15 min delay (FR-1.4 Fajr) + 45 min Jumuah dim + 10 min margin, so windows of adjacent slots cannot meet — this is a test-space bound, not a product claim); settings drawn as `adhan_duration_s = st.integers(60, 300)` then built explicitly as `Settings(masjid_name="Masjid Test", zone="SGR01", hijri_offset=0, adhan_duration_s=drawn, iqamah_rules=DEFAULT_IQAMAH_RULES)` (dim defaults 20/45, FR-1.4 delays); `now` drawn as `st.integers(0, 86399)` seconds-of-day combined onto the drawn date under the tz. Build a matching `PrayerDay` (`source=JAKIM`, `fetched_at=now−1h`, `zone="SGR01"`). Then five properties, each calling the pure `domain.resolve_next_event` directly (states/targets are domain output; engine passthrough is pinned by Task 3's fixture replay): `test_targets_are_monotonic` — for every drawn `now`: `event.now == now`; if `iqamah_at` is not None then `adhan_at <= iqamah_at`; if `dim_until` is not None then `iqamah_at is not None and iqamah_at <= dim_until`; state-consistency window per PRD §8: `ADHAN → adhan_at <= now`, `IQAMAH_COUNTDOWN → adhan_at + adhan_duration <= now < iqamah_at`, `SALAH_DIM → iqamah_at <= now < dim_until`, `PRE_ADHAN → adhan_at - 5min <= now < adhan_at`, `NORMAL → now < adhan_at`; `test_state_windows_do_not_overlap` — independent oracle built from PRD §8 (not copied from `prayer_state.py`): for each drawn instant compute membership in `[adhan−5m, adhan)`, `[adhan, adhan+dur)`, `[adhan+dur, iqamah)`, `[iqamah, dim)` across all six slots (Syuruq contributes only the first two, no iqamah/dim), assert at most one window contains the instant AND `resolve_next_event`'s state equals that window's state when one matches, else `NORMAL`; sample instants every 5 minutes across the day (288 resolves/example — all windows are ≥ 5 minutes at these bounds except the adhan overlay, whose sampled instants are still checked for correctness, not exhaustiveness); `test_syuruq_never_dims` — whenever `next_prayer is SYURUQ`: `iqamah_at is None`, `dim_until is None`, `state in {NORMAL, PRE_ADHAN, ADHAN}`; `test_midnight_resolves_next_day_fajr` — same `schedules(max_minute=1320)` (isha ≤ 22:00) so every `now` in `[23:00, 24:00)` is post-Isha-dim: `state is NORMAL`, `next_prayer is FAJR`, `adhan_at.date() == now.date() + 1 day`, `adhan_at.time() == today.fajr`, `iqamah_at == adhan_at + 15min` (called with `tomorrow=None` — the harder carry path; the supplied-twin path is unit-tested at `test_domain_prayer_state.py`); `test_resolution_is_idempotent` — same `(now, day, rules, settings)` twice → equal `NextEvent` (re-render idempotence). These properties pin already-landed 1A-2 domain code, so no red phase is available by construction: Run: `uv run pytest tests/property/test_state_machine_properties.py -v` → Expected: PASS. If any property fails it proves a real domain defect (or a wrong strategy bound) — stop, fix the domain or justify the bound in the PR; never weaken the invariant to get green. Verify: `uv run pytest -m property -q` → PASS (5 properties, `ci` profile 100 examples); `uv run pytest -q` → PASS.

**Depends on:** Task 1 (`DEFAULT_IQAMAH_RULES`); independent of Tasks 2–4 code; must land before Task 6's gate.

### Task 6: Purity scans, changelog, full exit gate

**Files:** `CHANGELOG.md` (modify), no source changes

- [ ] Run purity + layer scans: `rg -n "fastapi|sqlite3|httpx|datetime\.now|time\.time" src/muhideen/domain/ src/muhideen/engine/` → 0 hits (engine joins the purity discipline, `ARCHITECTURE.md:62`; wall-clock reads remain forbidden outside a future `adapters/system_clock.py`); `uv run lint-imports` → green (layer contract `pyproject.toml:79-88` already declares `muhideen.engine` above `adapters`/`domain`, now with real code; engine imports only `core` + `domain`).
- [ ] Extend `CHANGELOG.md` `[Unreleased]`: `### Added` — engine orchestration (slice 1A-4): `muhideen.engine.Engine` resolving day schedules through the FR-1.2 fallback chain over `PrayerRepo`/`SettingsRepo`/`CalcEngine`/`Clock` ports, computing §8 next events, publishing `state`/`tick` on the `EventBus`; integration tests on in-memory fakes; property tests for no-overlap/monotonic/Syuruq/midnight/idempotence invariants. `### Changed` — `Settings` gains `iqamah_rules` (FR-1.4 defaults) and `lat`/`lon`/`method` calc config (PRD §6.2); `PrayerRepo` gains `last_known(day, zone)` (FR-1.2 step 3, no adapter existed yet).
- [ ] Final verify — run in order, one Expected each: `uv run pytest -m unit -q` → PASS (79: 73 baseline + 6 from Task 1); `uv run pytest -m integration -q` → PASS (24); `uv run pytest -m property -q` → PASS (5); `uv run pytest -m contract -q` → PASS (30, untouched); `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run lint-imports && uv run pytest -q` → all green (pyright strict now covers the new `engine/` code; full suite = 103 baseline + 6 unit + 24 integration + 5 property = 138); `uv run pytest --cov=muhideen --cov-report=term-missing` → ≥95 (baseline 97.24%; every `engine.py` branch enumerated in Tasks 2–4 has a named test, `ARCHITECTURE.md:63`); `uv sync --locked --all-extras` → resolves (mirrors `ci.yml:18`); placeholder self-scan `rg -n "TBD|TODO|appropriate|similar to Task" docs/development/plans/2026-09-23-1a-4-engine-orchestration.md | rg -v "placeholder self-scan"` → 0 hits (the `-v` excludes this instruction line, which quotes the pattern). Paste all outputs in the PR description as exit-gate evidence.

**Depends on:** Tasks 1–5.

---

## Detailed-Plan Checklist

1. **PRD refs:** FR-1.2 fallback order (`PRD.md:78`), FR-1.4 iqamah defaults (`:80`), FR-1.6 monotonic countdowns (`:82`), FR-2.3/2.4/2.6 countdown/dim/state machine (`:90-93`), FR-4.3 SSE push + 60s poll (`:109`), §4.3 ports + in-memory use-case tests (`:163-166`), stale banner (`:206`), §6.2 settings keys (`:243-249`), §6.3 contract surfaces (`:304-312`), §8 machine + Syuruq/Jumuah/midnight edges (`:335-345`).
2. **Contract diff:** `shape: none`. Engine is internal orchestration: no DTO, fixture, or `docs/api-contract.md` change; no version bump (`docs/api-contract.md:65-66` untouched). `Settings`/`PrayerRepo` are not wire surfaces. The `state`/`tick` publish names are pinned to the already-contractual SSE event names (`docs/api-contract.md:41`) by literal-string tests. `CHANGELOG.md` entries still ship in this PR per repo convention (`CONTRIBUTING.md:58`), but no fixture/doc update is required because the contract did not change.
3. **Changes with ownership:** backend-owned — `src/muhideen/core/{values,ports,__init__}.py` (Settings aggregate + `last_known` port, user-approved design), `src/muhideen/engine/{engine,__init__}.py` (new), `tests/unit/test_core_{values,ports}.py`, new `tests/integration/test_engine.py` + `tests/property/test_state_machine_properties.py`, `CHANGELOG.md`. No `api/`, `adapters/`, `domain/`, `views/`, `themes/`, `migrations/`, `tools/` changes; frontend untouched (fixtures it builds against are unchanged).
4. **Tests by layer + new invariants:** `unit` 73→79 (Settings default rules = FR-1.4, calc-config defaults, coordinate guards, new fields frozen/hashable; `last_known` required by the `PrayerRepo` protocol); `integration` 0→24 (fallback ordering cached→calc→last-known with lazy skips; calc disabled by missing coords; calc exception → last-known; all-miss `ScheduleError(zone, date)`; zone stamping of calc days; single settings load per operation; fixture-vector replay engine→DTO byte parity; settings hot-reload; midnight tomorrow/calc/carry; stale propagation; `ConfigError` propagation; fan-out order `["state", "tick"]`, minute-tick, silent repeat, target-change → `state` only, no `config-update`); `property` 0→5 (monotonic targets + §8 state/target consistency, no-overlap oracle, Syuruq never dims, midnight next-day Fajr, idempotence); `contract` 30 untouched-green; `e2e` 0 (1A-7).
5. **Purity/import-linter impact:** `engine/` added to the purity scan (`fastapi|sqlite3|httpx|datetime.now|time.time` → 0 hits; time enters only via the `Clock` port and explicit `now` params). Layer contract unchanged — `pyproject.toml:79-88` already lists `muhideen.engine`; engine imports `core` + `domain` only (never `adapters`/`api`/`views`), `lint-imports` stays green. Strict pyright extends to `engine/` (no `type: ignore`; fingerprint fields explicitly annotated). No new dependency.
6. **Docs touched:** `CHANGELOG.md` (Added/Changed). `CONTEXT.md` unchanged — no new glossary terms (Fallback Chain/Stale/Calculation Method already defined `:42-54`). No ADR — no hard-to-reverse decision: the `Settings` extension is additive with defaults and test-pinned; the `PrayerRepo.last_known` addition happens before any adapter implements the port (grep: only test stubs). `docs/development/PHASES_AND_SLICES.md` unchanged (row already describes this slice).
7. **Rollback:** `git rm src/muhideen/engine/engine.py src/muhideen/engine/__init__.py tests/integration/test_engine.py tests/property/test_state_machine_properties.py`; `git checkout main -- src/muhideen/core/values.py src/muhideen/core/ports.py src/muhideen/core/__init__.py tests/unit/test_core_values.py tests/unit/test_core_ports.py`; drop this slice's `CHANGELOG.md` bullets. No migration (schema lands 1A-5), no contract version bump, no fixture revert (unchanged), no downstream code depends on the engine yet (nothing imports `muhideen.engine` outside its own tests).
8. **Exit gate command output:** paste `uv run pytest -m unit -q`, `-m integration -q`, `-m property -q`, `-m contract -q`, the full `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run lint-imports && uv run pytest -q`, the coverage line, the purity-scan `rg` result, `uv run lint-imports` output, and `uv sync --locked --all-extras` in the PR description; CI runs the same gate (`.github/workflows/ci.yml:18-24`).

## Out of Scope (later slices)

- SQLite adapter implementing the new port surfaces (`sqlite_repo` with `last_known` query, `SettingsRepo.load/save` reading `settings` + `iqamah_rules` rows, migration seeding FR-1.4 defaults) — 1A-5 (its entry, 1A-4, is this slice).
- JAKIM fetch/scheduler, calc library spike, `SyncError` retry/backoff — 1A-6. The engine deliberately does not hold a `JAKIMClient`: fetching writes `PrayerRepo` via the scheduler, it is not a per-tick read.
- HTTP handlers wiring `Engine` into `create_app()`, `SystemClock` adapter (first wall-clock site), SSE subscriber serializing `state`/`tick` via the 1A-3 DTOs, `config-update` publishing on admin saves, error→status mapping (`ScheduleError`→5xx), auth/rate limits — 1A-7.
- Admin `SALAH_DIM` skip (`PRD.md:342` edge) needs engine-side override state plus an admin endpoint — lands with admin endpoints (1A-7), flagged here so 1B-2 does not assume it exists.
- Heartbeat `last_seen` batching (`PRD.md:311`), `VACUUM INTO` backups — 1A-5; Hijri full calendar, visual state QA — 1B-2 / 1A-6; any `views/` or theme work — 1B.
