# Slice 1A-6 Schedule Sources Implementation Plan

> **For workers:** Execute task-by-task via `tdd` + `verification-before-completion`. Review gate: `muhideen-momus-review` (plan), `muhideen-oracle-review` (after impl).

**Goal:** Land the two schedule sources behind the existing ports — `adapters/jakim_esolat` (defensive `period=year` client per PRD §6.1: pinned UA/timeout, in-client backoff, adapter-side naming map, parse + ordering rejection, keep-cache-on-fail) and `adapters/calc_mabims` (MABIMS fallback deriving all 8 markers per recorded research) — plus the 02:00 scheduler with 5m/15m/1h retries, making the offline-first claim (PRD.md:38) true and closing every exit criterion of the slice row.

**Architecture:** `adapters/` gains three modules (all implement/compose `core` ports; `apscheduler` — pre-declared at `pyproject.toml:23` — finally used), `domain/` gains one pure validator (`ordering.py` — `values.py:9-14` reserves validation for domain), `core/ports.py` amends `JAKIMClient.fetch_week` → `fetch_year` (zero consumers verified: only `ports.py:52-53` + `tests/unit/test_core_ports.py:71-77`; port-precedent 1A-4 `PrayerRepo.last_known`, 1A-5 `DisplayRepo`). NOT changed: `engine/` (`_calc_day` at `engine.py:127-141` already consumes `CalcEngine` and stamps zone), `api/`, `views/`, `tools/`, themes, `migrations/` (no schema change), `docs/api-contract.md` + fixtures (contract diff: `none` — `docs/api-contract.md:68-69` versioning untouched). Production wiring (starting the scheduler, injecting `SystemClock`, install seed) is 1A-7/1A-8.

**Tech Stack:** Python 3.11+ (venv runs 3.13), uv, `httpx` promoted dev→runtime (purity gate already sanctions it for adapters: `CONTRIBUTING.md:46`, `.github/workflows/ci.yml:23-30`), `apscheduler>=3.10`, `adhanpy==1.0.5` (calc engine; MIT, zero runtime deps — Task 1 pin), ruff, strict pyright (no `type: ignore`, `CONTRIBUTING.md:51`), import-linter, pytest (`unit`/`contract`/`integration` markers; `property`/`contract` suites untouched-green), `httpx.MockTransport` so unit/contract/integration never touch the network.

**References:** `docs/development/PHASES_AND_SLICES.md:43` slice 1A-6 row (entry 1A-5 `:42`, unblocks offline-first claim / 1A-7 `:44`), `:78-80` plan checklist, `PRD.md:77` FR-1.1 (02:00 + 5m/15m/1h + 30d/7d cache + `STALE` >48h), `:78` FR-1.2 fallback chain, `:79` FR-1.3 (MABIMS primary fallback; DST via IANA), `:83` FR-1.7 (JAKIM's 8 markers map via adapter §6.1), `:146` rejected build tooling (uv-only deps), `:155` sync server locked, `:200` ≤150 MB RSS during JAKIM sync, `:206` background sync never blocks render, `:233-237` §6.1 JAKIM Sync (URL block `:235`, pinned-keys bullet `:237`), `:325` single process/service, `docs/adr/0001-backend-stack.md:10` FastAPI-sync decision, `docs/adr/0003-sqlite-behind-ports.md:10` ports + in-memory fakes, `ARCHITECTURE.md:34` Core ports, `:36-40` layers (`api → engine → adapters → domain → core`), `:43` adapters landed/pending paragraph, `:62` purity scans (adapters is the sanctioned `httpx` site), `:63` coverage 95, `TESTING_STRATEGY.md:9-13` test layers (integration = engine + fake JAKIM/calc), `:17-20` pinned `FakeClock` + file-local doubles, `:41` negative cases "join when schedule sources land", `:43` provenance `source` + `fetched_at`, `CONTRIBUTING.md:38-51` quality gate, `.github/workflows/ci.yml:18-34` CI gate, `src/muhideen/core/ports.py:52-53` `JAKIMClient.fetch_week` (rename), `:57-60` `CalcEngine`, `:64-67` `Clock`, `src/muhideen/core/errors.py:25-31` `SyncError(message, zone, date)`, `src/muhideen/core/values.py:25-41` `MarkerName`, `:76-80` `ScheduleSource`, `:83-98` `PrayerDay` (source `:96`, fetched_at `:97`), `src/muhideen/domain/fallback.py:11` `STALE_AFTER`, `:20-25` `is_stale` (48h or degraded source), `src/muhideen/domain/__init__.py:1-19` exports, `src/muhideen/engine/engine.py:113-141` lazy chain + `ValueError`→cache-miss + zone stamping, `src/muhideen/adapters/sqlite_repo.py:42-84` `Database` single-writer, `src/muhideen/api/dto.py:37-38` prayer/boundary literals (no DTO change), `tests/unit/test_core_ports.py:71-77` JAKIM stubs, `:124-133` `ALL_PROTOCOLS`, `:137`/`:155` isinstance parametrize, `tests/integration/test_engine.py:104-115` file-local `FakeClock`, `:423` clock-advance precedent, `tests/conftest.py:1-11` Hypothesis `ci` profile, `pyproject.toml:18-25` deps (`apscheduler:23`), `:35-42` markers, `:52` `fail_under = 95`, `:76-88` import-linter layers, `:98` dev-only `httpx` (promote), `docs/api-contract.md:68-71` versioning (untouched), `api/fixtures/prayer-day.json:1` (untouched), precedent plans `2026-09-22-1a-2-domain-pure-logic.md` (`:59`, `:101` defer Hijri library choice to 1A-6), `2026-09-23-1a-4-engine-orchestration.md:106` (engine deliberately holds no `JAKIMClient`; the scheduler owns fetching), `2026-09-23-1a-5-persistence.md` (repo stores, never validates — 1A-6 owns validation).

**Branch:** `feature/slice-1a-6-schedule-sources` (from `main` @ `d71bf57`, already created and checked out)

**Slice:** 1A-6 Schedule sources. Entry: 1A-5 landed (`d71bf57`); baseline verified on this branch: `uv run pytest -q` = **208 passed** (unit 95, contract 35, integration 70, property 8, e2e 0), coverage 99%, 2026-09-23. Exit → task map: parser-rejection tests → Task 3; ordering-validation tests → Tasks 2+3; retry tests → Tasks 3+5; stale-flag tests → Task 6; "no network in unit/contract" → MockTransport-only design (Task 3) + `rg` evidence (Task 6); "library choice recorded (spike first)" + "calc fallback derives Boundary markers per recorded research" → Tasks 1+4.

---

## Design Decisions (locked for this slice)

> **Amendments (2026-09-24, executor, maintainer-authorized "change the plan if needed"):**
> (1) the calc-library candidate list gains **`adhanpy`** (maintainer's suggestion; the original `adhan` candidate is a different, disqualified package) — Task 1's report evaluates four candidates and pins `adhanpy==1.0.5`;
> (2) the year-end rollover arithmetic is corrected from "Dec 28–31" to "**from Dec 02** the +30-day window runs out of rows" (the report is the authority per Task 1 — arithmetic in report §Fetch window);
> (3) `docs/development/AGENTS.md` forbids shipped docs referencing development-doc filenames, so Task 6's PRD/CHANGELOG edits **inline** the pinned values instead of linking the research report.
> (4) oracle round 1 (coverage evidence) found 5 defensive rejection raises in `jakim_esolat.py` with no named test, contradicting this plan's own Task 6 exit claim ("every rejection branch has a named test"): added 3 `test_rejects_malformed_date` params (`None` non-string, `"01-Jan"` wrong field count, `"32-Jan-2026"` calendar-invalid) + `test_rejects_row_that_is_not_an_object` + `test_rejects_conflicting_marker_spellings` — PASS-first-run guards over already-landed raises (Task 6 precedent). Pinned counts re-pinned: client 19 fns/28 items, integration 127, full 275, slice deltas +57/+67.
> (5) PR #12 review round (Sourcery ×5, CodeRabbit ×2 inline + review bodies; every thread triaged fix-now/skip with reasoning left in-thread): (a) `_parse_row_date` requires the exact `dd-Mmm-yyyy` shape — `re.fullmatch(r"\d{2}-[A-Za-z]{3,4}-\d{4}", value)` before the month map (`{3,4}` because `Ogos` is 4 chars), rejecting `1-Jan-2026` (unpadded day) and `01-Jan-26` (short year); (b) an empty `prayerTime` list now raises `SyncError` (a 200 with zero rows must not report success while saving nothing) — completeness checks beyond empty were skipped: no verified completeness invariant exists (`~365` is year-dependent), per-row validation + FR-1.2 cover partial payloads; (c) **any 4xx (incl. 429) fails fast** — log a warning, then `raise SyncError(...) from exc` with no in-client sleep: request-level rejections cannot heal inside a burst (the endpoint's WAF ban re-engages within seconds), so the FR-1.1 scheduler chain (5m/15m/1h) paces those retries — `test_client_error_statuses_use_same_backoff` renamed `test_4xx_fails_fast_without_in_client_retry`, parametrized `[404, 429]`, asserting `sleeps == []` and handler count 1; (d) `run_sync`'s save loop converts repository write failures into `SyncError(f"prayer repo write failed: {exc}", zone=settings.zone) from exc` so a locked/full database enters the retry chain instead of escaping `sync_job` — an atomic bulk save was skipped: `save_day` is a per-row idempotent short-transaction upsert, a retry rewrites the full year (self-healing), and FR-1.2 covers gaps; (e) the give-up log reports `attempt + 1` ("after 4 attempts" = the initial run + 3 retries); (f) every registered job passes `misfire_grace_time=None` + `coalesce=True` through `_add_job`/`_AddJob` (APScheduler's default grace is 1 s — a skipped due run would silently break the chain); (g) missing docstrings added to the 6 undocumented `src/` functions — test-function docstrings were skipped (ruff enables no `D` rules, CONTRIBUTING pins none); (h) this plan's report-section text corrected: "Dec 28–31 rollover limit" → "from Dec 02" (the only stale occurrence). Pinned counts re-pinned: client 20 fns/32 items, scheduler 11 fns/11 items, integration 132, full 280, slice deltas +62/+72; rejection paths 13 → 14.
> (6) PR #12 follow-up round — CodeRabbit re-reviewed `288ce2c` while round-(5) replies were still posting and raised one inline comment (thread `4086812214`, `jakim_esolat.py:145`): reject non-year or incomplete JAKIM payloads. Both premises verified before fixing: both committed fixtures pin `periodType: "year"` with 365 unique rows covering `01-Jan`…`31-Dis` of the fetched year (captured 2026-09-23, future rows included), and the research probe table shows `periodType` echoing the query (`period=week` → `"week"`, 8 rows). Fixes: (a) `parse_takwim` rejects `periodType != "year"` right after the status check (key absent → `None` → also rejected: schema drift must surface as `SyncError`, never `KeyError`); (b) after the row loop a leap-aware coverage guard — `len(dates) == len(set(dates)) == (date(fetched_at.year+1,1,1) - date(fetched_at.year,1,1)).days` and every date within `fetched_at.year` — forces exact coverage of the fetched calendar year (nothing missing, nothing duplicated, nothing beyond `31-Dis`, which stays FR-1.2's documented year-boundary job); (c) `test_parse_malay_month_tokens` now swaps the target date into row 0 instead of overwriting it (the old overwrite duplicated `01-Dis`/dropped `01-Jan` and would correctly trip the new guard; assertion strength unchanged). **Supersedes amendment (5)(b):** the completeness-skip premise ("no verified completeness invariant exists (`~365` is year-dependent)") is falsified — the expected count derives from `fetched_at.year` (leap-aware), and full-year coverage is verified by both fixtures plus `PRD.md:237` ("one call returns the whole calendar year"). New red-green guards: `test_rejects_non_year_period_type` (parametrize `"week"`/`"month"`/absent), `test_rejects_incomplete_year_payload` (parametrize truncated / duplicate date), `test_rejects_out_of_year_row` (foreign-year row) — all six added items red first, green after the guards. No PRD edit: the guards enforce facts `PRD.md:237` already pins (root `periodType`, whole-calendar-year response) under the existing "Reject + keep cache on parse fail". CHANGELOG "empty payloads" → "empty or incomplete payloads" (plan CHANGELOG pin updated to match). Pinned counts re-pinned: client 23 fns/38 items, scheduler 11 fns/11 items, integration 138 (trajectory 108 → 124 → 135 → 138), full 286, slice deltas +68/+78; rejection paths 14 → 16.

1. **The research report is the implementation spec (spike first, per the row).** Task 1 captures the payloads and writes `docs/development/research/2026-09-23-schedule-sources.md`. The following facts were verified against the live endpoint on 2026-09-23 and Task 1 records them with evidence: response root `{"prayerTime": [...], "status": "OK!", "zone": "...", "periodType": ...}`; each row `hijri, date, day, imsak, fajr, syuruk, dhuha, dhuhr, asr, maghrib, isha`; times `HH:MM:SS`; dates `dd-Mmm-yyyy` with Malay month tokens `Jan Feb Mac Apr Mei Jun Jul Ogos Sep Okt Nov Dis` (English `Mar/May/Aug/Dec` do **not** appear); `date=` is ignored; `period=year` returns the entire calendar year (365 rows, `01-Jan-…`→`31-Dis-…`), `period=week` = today..+7, `period=month` = whole current month. Boundary derivations from **both** SGR01 and KDH01 full-2026 tables (730 rows): `imsak = fajr − 10 min` exactly, every day; `dhuha = syuruk + per-zone constant` (+25 min SGR01, +27 min KDH01) — so calc needs one latitude rule fitted within the golden tolerance (Task 1 pins it); strict PRD ordering holds on all 730 rows. What Task 1 still decides (with recorded evidence): calc library + version + license + strict-pyright compatibility, MABIMS angle/Asr params fitted to the golden rows, golden tolerance (target 3 min, must be ≤5 min or the library is rejected), representative lat/lon per zone, the single dhuha rule + max residual, and the Hijri library recommendation (discharges `2026-09-22-1a-2-domain-pure-logic.md:59,101` — computation itself stays out of scope, see checklist 6).
2. **One `period=year` call IS the daily sync.** It covers FR-1.1's 30-forward/7-past window (and supersedes the "monthly prefetch loop": install's seed fetch, 1A-8, runs the same job once). Known limit pinned in report + PRD: rows stop at `31-Dis`, so from Dec 02 the +30-day forward window runs out of rows — days past 31-Dec fall to the FR-1.2 fallback chain until the next January fetch (and a first-ever install during Jan 01–07 has no previous-year rows for the 7-days-past side — same chain). Hence port amendment `fetch_week` → `fetch_year(zone) -> list[PrayerDay]`.
3. **Two retry tiers, both mandated by PRD.** Client tier (§6.1 "3 retries with backoff"): `RETRY_DELAYS_S = (2.0, 4.0, 8.0)` around 4 total attempts, injected `sleep`, uniform over 5xx/transport `httpx.HTTPError` + invalid JSON, but **any 4xx (incl. 429) fails fast** — log a warning, then `raise SyncError(...) from exc`, no in-client sleep (request-level rejections cannot heal inside a burst; the scheduler tier paces those retries); parse/validation failures raise `SyncError` immediately (no retry — reject + keep cache). Scheduler tier (FR-1.1): `RETRY_DELAYS_S = (300.0, 900.0, 3600.0)` = exactly the 5m/15m/1h chain, implemented as APScheduler `DateTrigger` jobs scheduled by a failing job, plus the `02:00` `CronTrigger`. Verified 2026-09-23: a **stopped** `BlockingScheduler` exposes pending jobs via `get_jobs()`, `str(job.trigger) == "cron[hour='2', minute='0']"`, `job.trigger.timezone` is comparable, `DateTrigger.run_date` is exact, and `job.func()` (a `functools.partial`) is directly callable — so every test is deterministic: no scheduler start, no threads, no wall-clock (`run_date = clock.now() + delay`); every registration passes `misfire_grace_time=None` + `coalesce=True` (a skipped due run would silently break the chain).
4. **Ordering validation is one pure domain function used by both adapters.** `domain/ordering.py::ensure_ordered(day) -> PrayerDay` raises `SyncError(message, zone, date)` (context fields `errors.py:28`) on any adjacent violation of `Imsak<Fajr<Syuruq<Dhuha<Dhuhr<Asr<Maghrib<Isha`. The repo still stores without validating (1A-5 contract, `PRD.md:237` is this slice); `core/values.py:9-14` reserves validation for `domain/`.
5. **Fetch fully, then write.** `fetch_year` returns only after the entire payload parses *and* validates, so a `SyncError` path performs zero `save_day` calls and the cache survives intact; the sync upserts only, never deletes.
6. **Dependency moves, not new machinery.** `httpx>=0.27` moves from dev-group (`pyproject.toml:98`) to `[project] dependencies` (already in `uv.lock`), `uv lock` in the same PR; `apscheduler` was pre-declared at `pyproject.toml:23` for exactly this slice. `USER_AGENT = "muhideen/0.1.0 (+https://github.com/nexusnv/muhideen)"` and `TIMEOUT_S = 15.0` are pinned constants (§6.1). One *new* runtime dependency: `adhanpy==1.0.5` (the report-pinned calc library; MIT, zero runtime deps, 17 kB wheel) — still no new machinery: it sits behind `CalcEngine` with golden-pinned outputs (swap = bounded rework, Decision 8).
7. **`MabimsCalcEngine` injects `Clock` + `tzinfo` in its constructor; the `CalcEngine` port does not change.** (The computation library behind it is the report-pinned `adhanpy==1.0.5` — MIT, zero deps, runtime dependency.) `compute_day(day, lat, lon, method)` keeps its signature (`ports.py:57-60`), engine untouched; `fetched_at = clock.now()`, `source = ScheduleSource.CALC`, `zone = ""` (engine stamps the zone, `engine.py:137-140`), unknown method raises `ValueError` → engine's existing cache-miss path (`engine.py:135`). IANA tz injection at composition (1A-7/1A-8) satisfies FR-1.3's DST rule; tests inject `ZoneInfo("Asia/Kuala_Lumpur")`.
8. **No ADR, no contract change, no migration.** The calc library sits behind `CalcEngine` with golden-pinned outputs (swap = bounded rework, not hard-to-reverse), the deps were pre-declared, gate-sanctioned, or (for `adhanpy`) a zero-dep MIT library pinned behind `CalcEngine` with golden-pinned outputs — the research report + changelog are the record (checklist 6: ADR only if hard-to-reverse). Contract diff `none`: no DTO, fixture, or `docs/api-contract.md` change (`:68-71` untouched), `source`/`stale` wire values already cover jakim/calc provenance (`api/fixtures/prayer-day.json:1`, `dto.py:37-38`).

## File Structure

- Create: `docs/development/research/2026-09-23-schedule-sources.md` — 9 sections: Endpoint behavior / Payload schema / Fetch window / Naming map / Boundary derivations / Ordering / Calc library spike / Golden vectors / Hijri
- Create: `tests/data/jakim_year_sgr01.json`, `tests/data/jakim_year_kdh01.json` — captured `period=year` payloads (evidence + test input; `tests/data/` holds no `test_*.py`, pytest ignores it)
- Create: `src/muhideen/domain/ordering.py` — `ORDER` (the 8 source markers) + `ensure_ordered(day) -> PrayerDay`
- Modify: `src/muhideen/domain/__init__.py` — re-export `ensure_ordered`, `ORDER`
- Create: `src/muhideen/adapters/jakim_esolat.py` — pinned consts, `SOURCE_TO_MARKER`, `MALAY_MONTHS`, `parse_takwim(payload, *, zone, fetched_at)`, `HttpJAKIMClient(clock, sleep=time.sleep, transport=None)` implementing `fetch_year`
- Create: `src/muhideen/adapters/calc_mabims.py` — `MabimsCalcEngine(clock, tz)` implementing `compute_day` (MABIMS only; derives `imsak`/`dhuha` per research report)
- Create: `src/muhideen/adapters/scheduler.py` — `RETRY_DELAYS_S = (300.0, 900.0, 3600.0)`, `MAX_RETRIES = 3`, `run_sync(...) -> int`, `sync_job(..., attempt=0)`, `_add_job` (`misfire_grace_time=None`, `coalesce=True`), `build_scheduler(...) -> BlockingScheduler` (not started)
- Modify: `src/muhideen/core/ports.py:52-53` — `fetch_week` → `fetch_year` + docstring ("one `period=year` call, ~365 rows")
- Modify: `tests/unit/test_core_ports.py:71-77` — `FullJAKIMClient.fetch_year` / `PartialJAKIMClient` rename (parametrize lists unchanged)
- Modify: `pyproject.toml` — `httpx>=0.27` into `[project] dependencies`, remove `:98` dev entry; `adhanpy==1.0.5` into `[project] dependencies` (Task 1 pin; already added during the spike); `uv lock`
- Test: `tests/unit/test_domain_ordering.py` — 4 functions / 10 items (pinned order, 7 parametrized violations, error context)
- Test: `tests/integration/test_jakim_esolat.py` — 23 functions / 38 items (parse 3 fns/4 items, reject 14 fns/25 items, retry 4 fns/7 items, log 1, port 1), all via `httpx.MockTransport`
- Test: `tests/integration/test_calc_mabims.py` — 6 functions / 16 items (golden vectors ×6, exact imsak ×6, ordering, unknown method, provenance, port)
- Test: `tests/integration/test_scheduler_sync.py` — 11 functions (cron, sync, retries, repo-write-failure, guards, logging)
- Test: `tests/integration/test_sync_stale.py` — 3 functions (fresh JAKIM, +49h, calc-sourced)
- Modify: `PRD.md:77` FR-1.1 sentence, `:235` URL `period=week`→`period=year`, `:237` bullet replaced by the pinned inventory (verbatim in Task 6)
- Modify: `ARCHITECTURE.md:43` landed/pending adapters paragraph; `TESTING_STRATEGY.md:41` negative-case sentence; `CHANGELOG.md:7` `[Unreleased]`

`src/muhideen/{engine,api,views,migrations}/`, `tools/`, `themes/`, `api/fixtures/`, `docs/api-contract.md`, `docs/development/PHASES_AND_SLICES.md` (row `:43` already describes this slice), `CONTEXT.md` (no term changes): **no change** (contract diff: `none` — checklist item 2).

---

### Task 1: Spike + research report + captured payloads

**Files:** `docs/development/research/2026-09-23-schedule-sources.md` (create), `tests/data/jakim_year_sgr01.json` + `tests/data/jakim_year_kdh01.json` (create)

Research task — evidence artifacts, verified by commands instead of a failing test.

- [ ] Capture both payloads with the project toolchain: `uv run python -c "import httpx,pathlib; r=httpx.get('https://www.e-solat.gov.my/index.php?r=esolatApi/takwimsolat&period=year&zone=SGR01', timeout=15, headers={'User-Agent':'muhideen/0.1.0'}); pathlib.Path('tests/data').mkdir(parents=True, exist_ok=True); pathlib.Path('tests/data/jakim_year_sgr01.json').write_bytes(r.content); print(r.status_code, len(r.json()['prayerTime']))"` → Expected: `200 365`; repeat with `zone=KDH01` → `200 365`. Validate: `uv run python -c "import json; d=json.load(open('tests/data/jakim_year_sgr01.json')); assert d['status']=='OK!' and len(d['prayerTime'])==365 and d['prayerTime'][0]['date'].endswith('-Jan-2026') and d['prayerTime'][-1]['date']=='31-Dis-2026'; print('payload ok')"` → `payload ok`. If the endpoint behaves differently than the Design-Decision-1 pins (endpoint may change without notice), record the delta in the report and adjust the Task 6 PRD text to match reality — the report is the authority.
- [ ] Write the report with exactly the nine `##` sections named in File Structure, containing: (a) probe log — the four verified request/response probes (week, month, week+date ignored, year) with capture date and status codes; (b) payload schema — root keys, row keys, `HH:MM:SS`, `dd-Mmm-yyyy` + the 12 Malay month tokens; (c) fetch window — `period=year` choice, FR-1.1 window coverage arithmetic, the from-Dec-02 rollover limit (the +30-day window runs out of rows); (d) naming map — the exact `SOURCE_TO_MARKER` table incl. PRD `PRD.md:237` variants; (e) boundary derivations — the `−10`/`+25`/`+27` evidence across both zones' 730 rows plus the fitted single dhuha rule with **max residual per zone** and the chosen representative lat/lon per zone (justify: zone reference point, tolerance absorbs intra-zone spread); (f) ordering — verified on all 730 rows; (g) Calc library spike — evaluate the four candidates `adhan` (PyPI 0.1.1, LGPLv3, 2015, custom `fajr_angle`/`isha_angle`/`asr_multiplier` params), `adhanpy` (PyPI 1.0.5, MIT, zero deps, port of batoulapps/adhan-java — added 2026-09-24 on the maintainer's suggestion), `islamic-times` (PyPI 3.1.0, MIT, but pulls `numpy`+`timezonefinder` — weigh against `PRD.md:200` RSS and `PRD.md:146` lean-toolchain), and a vendored PrayTimes.org single file (verify its license text) against four criteria: strict-pyright clean without `type: ignore` (`CONTRIBUTING.md:51`), MABIMS params expressible, golden fit ≤ tolerance, dependency weight — then **pin: library + version + license, MABIMS params, `GOLDEN_TOLERANCE_MIN` (target 3, hard ceiling 5), and per-marker mean/max error tables for both zones × 365 days**; (h) Golden vectors — the 6 test vectors (SGR01 `01-Jan-2026`, `01-Jun-2026`, `23-Sep-2026`; KDH01 `01-Jan-2026`, `01-Jun-2026`, `01-Dis-2026`) with provenance back to the committed JSON; (i) Hijri — recommended base-calendar library/table choice with license (discharges the 1A-2 deferral; computation lands with its consuming surface, see Out of Scope). Run: `rg -c "^## " docs/development/research/2026-09-23-schedule-sources.md` → Expected: `9`; `rg -n "GOLDEN_TOLERANCE_MIN|Library:|max residual|hijri" docs/development/research/2026-09-23-schedule-sources.md | wc -l` → ≥4; `uv run python -m json.tool tests/data/jakim_year_kdh01.json > /dev/null && echo ok` → `ok`.

**Depends on:** 1A-5 landed (entry). Unblocks Tasks 3 and 4.

### Task 2: Failing guard — `ensure_ordered` in `domain/ordering.py`

**Files:** `tests/unit/test_domain_ordering.py` (create), `src/muhideen/domain/ordering.py` (create), `src/muhideen/domain/__init__.py` (modify)

- [ ] Add failing tests (`pytestmark = pytest.mark.unit`, file-local `_day()` builder copying the `tests/unit/test_domain_fallback.py` `_day()` precedent): `test_ordered_day_passes_through` — a strictly ordered 8-marker `PrayerDay` returns equal to input; `test_every_adjacent_violation_raises_sync_error` (parametrize the 7 adjacent pairs with the two slots swapped — equal-times is covered by the next test, same `left < right` comparison) → `pytest.raises(SyncError)`; `test_equal_adjacent_times_raise` — `fajr == syuruq` rejected (PRD strict `<`); `test_error_carries_zone_and_date_context` — `exc.zone == "SGR01"` and `exc.date == "2026-09-23"` (`errors.py:28`), message names the offending markers (`"imsak"`/`"fajr"` text present). Run: `uv run pytest tests/unit/test_domain_ordering.py -v` → Expected: FAIL (`ModuleNotFoundError: ordering`).
- [ ] Implement `ensure_ordered(day: PrayerDay) -> PrayerDay` in `domain/ordering.py`: build `slots = ((MarkerName.IMSAK, day.imsak), (MarkerName.FAJR, day.fajr), (MarkerName.SYURUQ, day.syuruq), (MarkerName.DHUHA, day.dhuha), (MarkerName.DHUHR, day.dhuhr), (MarkerName.ASR, day.asr), (MarkerName.MAGHRIB, day.maghrib), (MarkerName.ISHA, day.isha))`, pairwise require `left < right`, raise `SyncError(f"time order violated: {left.value} {lv} !< {right.value} {rv}", zone=day.zone, date=day.date.isoformat())`; module constant `ORDER: tuple[MarkerName, ...]` = those 8 names in that order (consumed by Task 3's "missing marker" check); export both from `domain/__init__.py` (`:1-19`). Verify: same pytest command → PASS (4 functions, 10 collected items: 1 + 7 parametrized + 1 + 1); `uv run pytest -m unit -q` → PASS (105 = baseline 95 + 10 — pytest reports items, so parametrized cases each count); purity `rg -n "fastapi|sqlite3|httpx|datetime\.now|time\.time" src/muhideen/domain/` → 0 hits; `uv run lint-imports` → green (domain imports core only).

**Depends on:** none beyond entry (pure domain).

### Task 3: Failing guard — `HttpJAKIMClient` + `parse_takwim` + port rename

**Files:** `tests/integration/test_jakim_esolat.py` (create), `src/muhideen/adapters/jakim_esolat.py` (create), `src/muhideen/core/ports.py` (modify `:52-53`), `tests/unit/test_core_ports.py` (modify `:71-77`), `pyproject.toml` + `uv.lock` (modify)

- [ ] Add failing tests (`pytestmark = pytest.mark.integration`; load `tests/data/jakim_year_sgr01.json` via `Path(__file__).resolve().parents[2] / "tests" / "data"`; file-local `FakeClock` + sleep recorder `sleeps.append` per `TESTING_STRATEGY.md:19`; **every** client test wires `httpx.MockTransport(handler)` — zero live URLs in tests): parse — `test_parse_year_payload_returns_365_prayer_days`, `test_parse_pins_recorded_marker_values_and_provenance` (row `23-Sep-2026`: `fajr == time(5,55)`, `dhuhr == time(13,9)`, `isha == time(20,20)`, `source is ScheduleSource.JAKIM`, `fetched_at == pinned clock.now()`, `zone == "SGR01"`), `test_parse_malay_month_tokens` (parametrize `("01-Dis-2026", date(2026,12,1))`, `("15-Ogos-2026", date(2026,8,15))`; the mutation swaps row 0's date with the row already carrying `date_text`, preserving unique full-year coverage — an overwrite would duplicate one day and drop another, tripping the coverage guard); rejection — each direct `parse_takwim(...)` call → `pytest.raises(SyncError)`: `test_rejects_status_not_ok` (`status="Zone not found"`), `test_rejects_missing_prayer_time`, `test_rejects_empty_prayer_time` (empty `prayerTime` list), `test_rejects_row_missing_marker` (drop `isha`), `test_rejects_malformed_time` (parametrize `"5:55"`, `"25:00"`, `"05:5x"`), `test_rejects_malformed_date` (parametrize `"31-Dez-2026"`, `"2026-09-23"`, `None` non-string, `"01-Jan"` wrong field count, `"1-Jan-2026"` unpadded day, `"01-Jan-26"` short year, `"32-Jan-2026"` calendar-invalid), `test_rejects_zone_mismatch` (payload `zone="KDH01"` requested `SGR01`), `test_rejects_ordering_violation` (row with `isha="05:00"` → the domain `ensure_ordered` `SyncError`), `test_rejects_non_object_payload` (`payload=["nope"]`), `test_rejects_row_that_is_not_an_object` (`prayerTime[2]="not-a-row"`), `test_rejects_conflicting_marker_spellings` (row gains the PRD variant `subuh` alongside `fajr`), plus amendment-(6)'s red-green guards: `test_rejects_non_year_period_type` (parametrize `periodType` `"week"` / `"month"` / absent `None`), `test_rejects_incomplete_year_payload` (parametrize truncated last 10 rows / duplicated date), `test_rejects_out_of_year_row` (row dated `01-Jan-2025`) — the amendment-(4) 2 functions + 3 date params are PASS-first-run guards over already-landed defensive raises (Task 6 precedent; oracle round-1 coverage finding), while `test_rejects_empty_prayer_time` and the 4xx fail-fast test are red-green guards for amendment (5)'s new raises; retry — `test_transient_500_then_success_records_backoff_sleeps` (handler 500,500,200 → result parses, `sleeps == [2.0, 4.0]`), `test_exhausted_failures_raise_sync_error_after_three_retries` (parametrize always-500 / always-`httpx.ConnectTimeout` / 200-with-invalid-JSON-body → `SyncError`, `sleeps == [2.0, 4.0, 8.0]`, handler call count == 4), `test_4xx_fails_fast_without_in_client_retry` (parametrize 404, 429 → `SyncError`, `sleeps == []`, handler count == 1 — no in-client backoff for request-level rejections), `test_parse_rejection_after_200_does_not_retry` (200 with a row missing `isha` → `SyncError`, `sleeps == []`, handler count == 1); observability — `test_failure_logs_zone_and_status` (`caplog.at_level(logging.WARNING, logger="muhideen.adapters.jakim_esolat")`, always-500 → 4 records, first contains `zone=SGR01` and `status=500`); port — `test_http_jakim_client_satisfies_port` (`isinstance(HttpJAKIMClient(clock=fc), JAKIMClient)`). Run: `uv run pytest tests/integration/test_jakim_esolat.py -v` → Expected: FAIL (`ImportError: jakim_esolat`); `uv run pytest tests/unit/test_core_ports.py -v` → still PASS while `fetch_week` exists (rename lands with the implementation below).
- [ ] Implement `adapters/jakim_esolat.py`: constants `URL_TEMPLATE = "https://www.e-solat.gov.my/index.php?r=esolatApi/takwimsolat&period=year&zone={zone}"` (build with `URL_TEMPLATE.format(zone=quote(zone, safe=""))` — zone is never interpolated raw), `USER_AGENT`, `TIMEOUT_S = 15.0`, `RETRY_DELAYS_S = (2.0, 4.0, 8.0)`, `SOURCE_TO_MARKER` = observed keys `{imsak, fajr, syuruk, dhuha, dhuhr, asr, maghrib, isha}` + PRD `PRD.md:237` variants `{subuh→fajr, zohor→dhuhr, isyak→isha, duha→dhuha}`, `MALAY_MONTHS` (12 Malay tokens + English aliases), module `logger = logging.getLogger(__name__)`. `parse_takwim(payload: object, *, zone: str, fetched_at: datetime) -> list[PrayerDay]`: non-dict payload / `status` not `str` whose `rstrip("!") != "OK"` / `payload["zone"] != zone` / `prayerTime` not a `list` / empty `prayerTime` list → `SyncError` (a 200 with zero rows must not report success while saving nothing); `periodType` absent or != `"year"` → `SyncError` (a `period=week/month` payload echoes its own periodType — saving it would report a successful year sync while writing only a window); after the rows: leap-aware coverage guard — `len(dates) == len(set(dates)) ==` days in `fetched_at.year` and every date within `fetched_at.year`, else `SyncError` (truncated/duplicate/foreign-year payloads must not report success while leaving cache holes; nothing beyond `31-Dis` is required — FR-1.2 owns the year boundary); per row: non-dict → `SyncError`; iterate `row.items()`, keys absent from `SOURCE_TO_MARKER` are ignored (incl. `hijri`, `day`), values must fullmatch `\d{2}:\d{2}(:\d{2})?` and construct a stdlib `time` (range errors → `SyncError`), conflicting spellings of one marker → `SyncError`; date must `re.fullmatch(r"\d{2}-[A-Za-z]{3,4}-\d{4}", value)` (exact `dd-Mmm-yyyy` — rejects unpadded day `1-Jan-2026` and short year `01-Jan-26`; `{3,4}` because `Ogos` is 4 chars) before the month map (unknown token → `SyncError`); after the row, every name in `ORDER` must be present else `SyncError` listing the missing markers; build `PrayerDay(source=ScheduleSource.JAKIM, fetched_at=fetched_at)` and pass through `ensure_ordered`; return the row list. `HttpJAKIMClient`: `__init__(self, *, clock: Clock, sleep: Callable[[float], None] = time.sleep, transport: httpx.BaseTransport | None = None)`; `fetch_year` runs the verified loop — up to 4 attempts; per attempt: `client.get(url, headers={"User-Agent": USER_AGENT}, timeout=TIMEOUT_S)` + `raise_for_status()` + `resp.json()` inside `try/except (httpx.HTTPError, ValueError)` → `logger.warning("jakim fetch failed zone=%s status=%s attempt=%d: %s", ...)` (status from `HTTPStatusError.response.status_code`, else `"n/a"`), `last = exc`, then the 4xx fail-fast branch — `isinstance(exc, httpx.HTTPStatusError) and 400 <= status < 500` → `raise SyncError(f"jakim fetch rejected with HTTP {status}", zone=zone) from exc` (no sleep: request-level rejections cannot heal inside a burst; the FR-1.1 chain paces those retries), otherwise `sleep(RETRY_DELAYS_S[attempt])` when `attempt < 3`, continue; success → `return parse_takwim(payload, zone=zone, fetched_at=self._clock.now())` (a `SyncError` from parse escapes immediately — no retry, no writes); exhausted → `raise SyncError(f"jakim fetch failed after {1 + len(RETRY_DELAYS_S)} attempts", zone=zone) from last`. Rename the port method to `fetch_year` with the docstring above; rename `FullJAKIMClient`/`PartialJAKIMClient` methods (`test_core_ports.py:71-77` — `ALL_PROTOCOLS` `:124` and both parametrize lists `:137`/`:155` keep their counts). Dependency move: edit `pyproject.toml` per Design Decision 6, run `uv lock`. Verify: `uv run pytest tests/integration/test_jakim_esolat.py -v` → PASS (23 functions, 38 items — the seven parametrized tests add 15); `uv run pytest -m unit -q` → PASS (105, stubs renamed); `uv run pytest -m integration -q` → PASS (108 = baseline 70 + 38); `uv run pytest -m contract -q` → PASS (35, untouched); `uv run pyright` → green (every payload value converted to stdlib types before `PrayerDay`); `rg -n "e-solat\.gov\.my" tests/` → 0 hits (no live endpoint in tests).

**Depends on:** Task 1 (payload file), Task 2 (`ensure_ordered`).

### Task 4: Failing guard — `MabimsCalcEngine` behind `CalcEngine`

**Files:** `tests/integration/test_calc_mabims.py` (create), `src/muhideen/adapters/calc_mabims.py` (create), `pyproject.toml` + `uv.lock` (modified — the report pinned the PyPI library `adhanpy==1.0.5`)

- [ ] Add failing tests (`pytestmark = pytest.mark.integration`; module-level loader reads the two `tests/data/jakim_year_*.json` files, extracts the expected row by its `dd-Mmm-yyyy` string, converts `HH:MM:SS` → `time`; expected `GOLDEN_TOLERANCE_MIN` constant set to the report's pinned value — plan default `3`, with a comment citing the report section): `test_golden_vectors_match_recorded_jakim_times` (parametrize the 6 vectors named in File Structure: `(file, date-string, lat, lon, tz)` with the report's representative coordinates — assert **each of the 8 markers** within `GOLDEN_TOLERANCE_MIN` of the recorded value), `test_imsak_is_exactly_fajr_minus_ten_minutes` (parametrize the same 6 vectors: `calc.imsak == calc.fajr - timedelta(minutes=10)` — the exact rule from Decision 1, stronger than tolerance), `test_outputs_pass_ordering_validation` (`ensure_ordered(day) is day` for all 6, one function looping internally), `test_unknown_method_raises_value_error` (`method="ISNA"` → `ValueError` — the engine's documented cache-miss input, `engine.py:135`), `test_compute_day_stamps_calc_source_and_fetched_at` (`source is ScheduleSource.CALC`, `fetched_at == pinned clock.now()` — provenance `TESTING_STRATEGY.md:43`), `test_calc_mabims_satisfies_port` (`isinstance(MabimsCalcEngine(clock=fc, tz=TZ), CalcEngine)`). Run: `uv run pytest tests/integration/test_calc_mabims.py -v` → Expected: FAIL (`ImportError: calc_mabims`).
- [ ] Confirm the report-pinned dependency (`adhanpy==1.0.5` — already in `pyproject.toml`/`uv.lock` from Task 1's spike) and implement `MabimsCalcEngine`: `__init__(self, *, clock: Clock, tz: tzinfo)`; `compute_day(self, day: date, lat: float, lon: float, method: str) -> PrayerDay` — `method != "MABIMS"` → `ValueError(f"unsupported calculation method: {method}")`; compute sunset/sunrise (syuruq), transit (dhuhr), fajr/isha from the report-pinned MABIMS angles, asr from the report-pinned Asr factor, all in the injected `tz` (IANA in production, `ZoneInfo` in tests — no wall-clock, no `datetime.now`), then derive boundaries exactly as pinned: `syuruq` from the library's sunrise, `imsak = fajr − 10 min`, `dhuha = syuruq + <report-pinned latitude rule>`; convert every library return to stdlib `time` immediately (keeps strict pyright green without `type: ignore`); run `ensure_ordered` before returning (defensive — a violated invariant raises `SyncError`, never a silently bad schedule); stamp `source=ScheduleSource.CALC`, `fetched_at=self._clock.now()`, `zone=""` (engine stamps it, `engine.py:137-140`). Verify: `uv run pytest tests/integration/test_calc_mabims.py -v` → PASS (6 functions, 16 items — golden ×6 + imsak ×6 parametrized); `uv run pytest -m integration -q` → PASS (124 = 108 + 16); `uv run pytest -m unit -q` → PASS (105, untouched); `uv run pyright && uv run lint-imports` → green (adapters import `core` + `domain`, never `engine`/`api`); paste the golden test's per-marker deviations in the PR description as evidence the fit matches the report.

**Depends on:** Task 1 (library, params, tolerance, coordinates, dhuha rule), Task 2 (`ensure_ordered`).

### Task 5: Failing guard — 02:00 scheduler + FR-1.1 retry chain

**Files:** `tests/integration/test_scheduler_sync.py` (create), `src/muhideen/adapters/scheduler.py` (create)

- [ ] Add failing tests (`pytestmark = pytest.mark.integration`; file-local doubles only: `FakeClock` (pinned `2026-09-24T01:30+08:00`, advance-by-assignment per `test_engine.py:104-115`/`:423`), `FakeSettingsRepo`, `FakePrayerRepo` (records `save_day` calls; `save_error: Exception | None` param), `FakeJAKIMClient` (returns preset days or raises `SyncError`, records `last_zone`), `RecordingBus` not needed — no engine here): `test_build_scheduler_registers_daily_0200_cron` — build with the pinned clock → `scheduler.get_jobs()` has exactly one job with `id == "jakim-sync"`, `str(job.trigger) == "cron[hour='2', minute='0']"`, `job.trigger.timezone == TZ`, `job.misfire_grace_time is None`, `job.coalesce is True`, scheduler **not started**; `test_unaware_clock_is_rejected` — clock whose `now()` is naive → `pytest.raises(ValueError, match="tz-aware")` (guards the tz derivation, Decision 7); `test_run_sync_saves_every_day_for_the_configured_zone` — fake client returns 2 days, settings zone `SGR01` → returns `2`, `client.last_zone == "SGR01"`, both days saved; `test_run_sync_with_empty_year_returns_zero` — client returns `[]` → `0`, no saves; `test_run_sync_before_setup_raises_config_error` — settings repo raises `ConfigError` → propagates out of `run_sync`; `test_repo_write_failure_schedules_retry` — `FakePrayerRepo(save_error=OSError("disk full"))` → `run_sync` raises `SyncError("prayer repo write failed: …", zone=…)` chained from the `OSError`, then `sync_job` schedules `jakim-sync-retry-1` (a locked/full database must enter the FR-1.1 chain, not escape the job); `test_failure_keeps_cache_and_schedules_first_retry` — seed one cached row, client raises `SyncError("boom", zone="SGR01")` → `sync_job(...)` returns `None`, no `save_day` calls (seed row untouched), job `jakim-sync-retry-1` exists with `trigger.run_date == clock.now() + timedelta(seconds=300)`, `misfire_grace_time is None`, `coalesce is True`; `test_chained_failures_schedule_15m_then_1h` — call `retry-1.func()` with a still-failing client → `jakim-sync-retry-2` `run_date == clock.now() + 900`; call `retry-2.func()` → `jakim-sync-retry-3` `run_date == clock.now() + 3600` (FR-1.1 `PRD.md:77` chain, all absolute against the pinned clock — no wall-time); `test_gives_up_after_three_retries` — call `retry-3.func()` failing → no `retry-4` job and caplog contains `gave up after 4 attempts` (the initial run + 3 retries — `attempt + 1` in the log line); `test_job_logs_config_error_without_retry` — settings repo raises `ConfigError`, invoke the cron job's `func()` → returns `None`, no retry jobs, caplog contains `setup`/`ConfigError` text; `test_failure_logs_zone_and_attempt` — failing client → caplog record contains `zone=SGR01` and `attempt=1`. Run: `uv run pytest tests/integration/test_scheduler_sync.py -v` → Expected: FAIL (`ImportError: scheduler`).
- [ ] Implement `adapters/scheduler.py`: constants `RETRY_DELAYS_S = (300.0, 900.0, 3600.0)`, `MAX_RETRIES = 3`, logger; `run_sync(*, client: JAKIMClient, prayer_repo: PrayerRepo, settings_repo: SettingsRepo, clock: Clock) -> int` = `settings = settings_repo.load()` (first-boot `ConfigError` propagates — caught by the job wrapper), `days = client.fetch_year(settings.zone)`, save each inside `try/except (ConfigError, SyncError): raise` + `except Exception as exc: raise SyncError(f"prayer repo write failed: {exc}", zone=settings.zone) from exc` (repository write failures become retryable instead of escaping `sync_job`; each `save_day` is an independent upsert so a partial write self-heals on the next attempt), `return len(days)` (parse happens fully client-side, so any failure saved nothing — Decision 5); `sync_job(*, client, prayer_repo, settings_repo, clock, scheduler, attempt: int = 0) -> int | None` = try `run_sync`; `except ConfigError as exc:` → `logger.warning("jakim sync skipped (setup incomplete): %s", exc)`, return `None` (no retry); `except SyncError as exc:` → if `attempt < MAX_RETRIES`: `tz = clock.now().tzinfo`, `scheduler.add_job(functools.partial(sync_job, client=client, prayer_repo=prayer_repo, settings_repo=settings_repo, clock=clock, scheduler=scheduler, attempt=attempt + 1), DateTrigger(run_date=clock.now() + timedelta(seconds=RETRY_DELAYS_S[attempt]), timezone=tz), id=f"jakim-sync-retry-{attempt + 1}", replace_existing=True)` through the shared `_add_job` (which also passes `misfire_grace_time=None` + `coalesce=True` — a late wake still retries, and a skipped retry would break the chain) plus `logger.warning("jakim sync failed zone=%s attempt=%d: %s", exc.zone, attempt + 1, exc)`; else `logger.warning("jakim sync gave up after %d attempts (zone=%s): %s", attempt + 1, exc.zone, exc)` (reports `attempt + 1` — the initial run + 3 retries = 4 attempts), return `None`; `build_scheduler(*, client, prayer_repo, settings_repo, clock) -> BlockingScheduler` = derive `tz = clock.now().tzinfo`, raise `ValueError("clock must be tz-aware")` when `None`, construct `BlockingScheduler(timezone=tz)`, add the cron job `functools.partial(sync_job, ..., attempt=0)` on `CronTrigger(hour=2, minute=0, timezone=tz)` with `id="jakim-sync"` through the shared `_add_job` (`replace_existing=True`, `misfire_grace_time=None`, `coalesce=True`), and **return without starting** (1A-7 starts it in a daemon thread — `PRD.md:206` background sync never blocks render; single process preserved, `PRD.md:325`). Verify: `uv run pytest tests/integration/test_scheduler_sync.py -v` → PASS (11 functions, 11 items — no parametrize here); `uv run pytest -m integration -q` → PASS (135 = 124 + 11); `rg -n "datetime\.now|time\.time" src/muhideen/adapters/` → 0 hits (every instant comes from the injected `Clock`).

**Depends on:** Task 3 (`fetch_year` port shape; tests keep their own `FakeJAKIMClient` per `TESTING_STRATEGY.md:19`).

### Task 6: Stale-flag tests + docs sweep + changelog + full exit gate

**Files:** `tests/integration/test_sync_stale.py` (create), `PRD.md` (modify `:77`, `:235`, `:237`), `ARCHITECTURE.md` (modify `:43`), `TESTING_STRATEGY.md` (modify `:41`), `CHANGELOG.md` (modify `:7`)

- [ ] Add stale-flag acceptance tests (`pytestmark = pytest.mark.integration`; file-local fakes only — `FakeSettingsRepo`/`FakePrayerRepo`/`FakeClock`/`RecordingBus` duplicated per `TESTING_STRATEGY.md:19`, harness style copied from `tests/integration/test_engine.py:167-190`): `test_fresh_jakim_day_is_not_stale` — `run_sync` writes JAKIM days stamped with the pinned `clock.now()`, then `Engine(...).resolve_day(today, "SGR01", now).stale is False`; `test_jakim_day_older_than_48h_is_stale` — same harness, advance `clock.current` by `49h` (precedent `test_engine.py:423`) and re-resolve the same day → `stale is True` (`fallback.py:11,20-25`, FR-1.1 `PRD.md:77`); `test_calc_sourced_day_is_stale` — save a day with `source=ScheduleSource.CALC, fetched_at=now` → resolve → `stale is True` (degraded-source rule, `TESTING_STRATEGY.md:43`). Run: `uv run pytest tests/integration/test_sync_stale.py -v` → Expected: **PASS on first run** — these are acceptance tests composing already-landed Task 3/5 output with the existing engine (`run_sync` + `resolve_fallback`/`is_stale`), not a red-green guard; a FAIL pinpoints a defect in Task 3/5 output, which gets fixed in that task's file, not here. Implement nothing new in this task — then `uv run pytest -m integration -q` → PASS (138 = 135 + 3).
- [ ] **PRD.md verbatim edits:** (1) `:77` FR-1.1 — replace `Monthly prefetch loop at install seeds ~365 days.` with ``A single `period=year` fetch at install seeds ~365 days (one call returns the whole calendar year; the endpoint ignores `date=` — see §6.1).`` (2) `:235` — replace `period=week` with `period=year` in the sample URL. (3) `:237` — replace the whole bullet with: ``* Prefer `period=year` — one call returns the whole calendar year (~365 rows; verified 2026-09-23 on SGR01 + KDH01), covering FR-1.1's 30-days-forward/7-days-past cache; `period=week` = today..+7 and `period=month` = the whole current month, and the `date` parameter is ignored — arbitrary-window fetches are impossible, and rows stop at `31-Dis`, so from Dec 02 the +30-day forward window runs out of rows and days past 31-Dec fall to the FR-1.2 fallback chain until the next January fetch. Timeout 15s, UA pinned; 3 retries with backoff (2s, 4s, 8s) for 5xx/transport failures and invalid JSON — any 4xx (incl. 429) fails fast after the first request (no in-client sleep), because request-level rejections cannot heal inside a burst and the FR-1.1 scheduler chain (5m/15m/1h) paces those retries. Response root: `{"prayerTime": [...], "status": "OK!", "zone": ..., "periodType": ...}`; each row carries `hijri`, `date` (`dd-Mmm-yyyy` with Malay month tokens `Jan Feb Mac Apr Mei Jun Jul Ogos Sep Okt Nov Dis`), `day`, and all 8 markers as `HH:MM:SS` (`imsak, fajr, syuruk, dhuha, dhuhr, asr, maghrib, isha`). Validate `HH:MM[:SS]` + sane ordering (`Imsak < Fajr < Syuruq < Dhuha < Dhuhr < Asr < Maghrib < Isha`). Backend naming is canonical: the adapter maps source spellings (e.g. `subuh→fajr`, `zohor→dhuhr`, `isyak→isha`, `syuruk→syuruq`, `duha→dhuha`) to `MarkerName`. Boundary derivations pinned by source research over both zones' full-2026 tables: `imsak = fajr − 10 min` exactly; `dhuha = syuruk + per-zone offset` (+25 min SGR01, +27 min KDH01) — `calc_mabims` fits the single latitude-based rule `offset_min = round(22.9851 + 0.6555 × latitude)` within the pinned golden tolerance of 3 minutes. Reject + keep cache on parse fail. Log zone + HTTP status. Endpoint may change without notice — admin can switch to calc-only mode.`` (Task 1's report is the authority for the pinned numbers — reconcile before merging.)
- [ ] **ARCHITECTURE.md:43** — replace the paragraph with: ``One module per port, plus the migration runner. Landed: `sqlite_repo` (connection, single-writer `Database`, the prayer/settings/display repos, `VACUUM INTO` backup), `migrate`, `jakim_esolat` (defensive `period=year` client: pinned UA/timeout, in-client backoff, adapter-side naming map, parse + ordering rejection, keep-cache on fail), `calc_mabims` (MABIMS fallback behind `CalcEngine`, golden-tested against recorded JAKIM tables), `scheduler` (02:00 cron + 5m/15m/1h retries over APScheduler, injected `Clock`, never started outside 1A-7 wiring). Pending with their ports: `system_clock`/`fake_clock`, `sse_bus`, `cec`, and the MWL/ISNA/Egyptian calc methods (FR-1.3). Data tables live beside logic (`adapters/data/`), never inside presentation.``
- [ ] **TESTING_STRATEGY.md:41** — replace the sentence `JAKIM payload-parse rejection and \`TIME UNSYNCED\` join when schedule sources and device integration land.` with ``JAKIM payload-parse rejection, ordering validation, and sync retry/backoff are asserted since slice 1A-6; `TIME UNSYNCED` joins when device integration lands.``
- [ ] **CHANGELOG.md** `[Unreleased]`: `### Added` — schedule sources (slice 1A-6): defensive JAKIM e-solat client (`period=year`, pinned UA/15s timeout, 2s/4s/8s in-client backoff for 5xx/transport failures — any 4xx incl. 429 fails fast, paced by the 02:00 chain —, adapter-side naming map, parse + ordering rejection incl. empty or incomplete payloads keeping cache intact); `MabimsCalcEngine` MABIMS fallback deriving all 8 markers per recorded research (golden-tested against captured JAKIM year tables); 02:00 scheduler with FR-1.1 5m/15m/1h retry chain (injected `Clock`, APScheduler, never started in tests); `domain.ordering.ensure_ordered`; captured payloads `tests/data/` + source research recording the MABIMS params, the dhuha latitude rule, and the golden tolerance. `### Changed` — **migration note:** `core.ports.JAKIMClient.fetch_week` renamed `fetch_year` (one `period=year` call covers FR-1.1's window; zero consumers outside this slice); `httpx>=0.27` promoted from the dev group to runtime dependencies (the client needs it at runtime; already in the lockfile); added `adhanpy==1.0.5` (MIT, zero runtime deps) as the MABIMS calc engine library.
- [ ] Final verify — run in order, one Expected each: `uv run pytest -m unit -q` → PASS (105); `uv run pytest -m contract -q` → PASS (35, untouched); `uv run pytest -m integration -q` → PASS (138); `uv run pytest -m property -q` → PASS (8, untouched); `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run lint-imports && uv run pytest -q` → all green (full suite = 286 = baseline 208 + 78 new items: unit +10, integration +68 — pytest counts collected items, parametrize included); `uv run pytest --cov=muhideen --cov-report=term-missing` → ≥95 (baseline 99%; every new branch — both retry tiers' exhaust/give-up paths, the 4xx fail-fast branch, the save-loop repo-write conversion, every rejection branch, tz guard, `misfire_grace_time=None`/`coalesce=True` registration — has a named test, `ARCHITECTURE.md:63`); purity scans `rg -n "fastapi|httpx|datetime\.now|time\.time" src/muhideen/domain/ src/muhideen/engine/ src/muhideen/core/` → 0 hits, `rg -n "sqlite3" src/muhideen/domain/ src/muhideen/engine/ src/muhideen/core/ tests/unit/` → 0 hits, `rg -n "datetime\.now|time\.time" src/muhideen/adapters/` → 0 hits; `uv run lint-imports` → green (adapters now import `domain` + `core`, both allowed by `pyproject.toml:76-88`); network evidence `rg -n "e-solat\.gov\.my" tests/` → 0 hits; `uv sync --locked --all-extras` → resolves (lock updated with the `httpx` promotion + `adhanpy`, mirrors `ci.yml:18`); `uv run tools/lint_theme.py --theme classic-green` → ok (`ci.yml:34`, unchanged by this slice); placeholder self-scan `rg -n "TBD|TODO|appropriate|similar to Task" docs/development/plans/2026-09-23-1a-6-schedule-sources.md | rg -v "placeholder self-scan"` → 0 hits (the `-v` excludes this instruction line, which quotes the pattern). Paste all outputs in the PR description as exit-gate evidence.

**Depends on:** Tasks 1–5.

---

## Detailed-Plan Checklist

1. **PRD refs:** FR-1.1 02:00/backoff/30d-7d/`STALE` (`PRD.md:77`), FR-1.2 fallback chain (`:78`), FR-1.3 MABIMS + IANA DST (`:79`), FR-1.7 adapter naming map (`:83`), lean toolchain (`:146`), sync stack locked (`:155`), RSS during sync (`:200`), sync never blocks render (`:206`), §6.1 endpoint + validation + ordering + keep-cache (`:233-237`), single process (`:325`).
2. **Contract diff:** `none` — no DTO/fixture/`docs/api-contract.md` change, no version bump (`docs/api-contract.md:68-71` untouched); `source`/`stale` wire values already carry jakim/calc provenance (`api/fixtures/prayer-day.json:1`). PRD §6.1 is a requirements doc, not the wire contract; `CHANGELOG.md` entries still ship per `CONTRIBUTING.md:58`.
3. **Changes with ownership:** backend-owned — new `adapters/{jakim_esolat,calc_mabims,scheduler}.py`, new `domain/ordering.py`, `core/ports.py` rename, five new test files, `pyproject.toml`/`uv.lock`, docs sweep (`PRD.md`, `ARCHITECTURE.md:43`, `TESTING_STRATEGY.md:41`), research report + `tests/data/`, `CHANGELOG.md`. Untouched: `engine/`, `api/`, `views/`, `tools/`, `themes/`, fixtures, contract doc, migrations, `sqlite_repo.py`. Frontend unaffected (fixtures unchanged; 1B work continues against them).
4. **Tests by layer + new invariants:** `unit` 95→105 (+10 items from 4 ordering functions: 1 + 7 parametrized violations + 1 + 1; strict ordering invariant with `SyncError` context); `contract` 35 and `property` 8 untouched-green; `integration` 70→138 (+68 items from 43 functions: client 23 fns/38 items, calc 6 fns/16 items, scheduler 11 fns/11 items, stale 3 fns/3 items — pytest reports collected items, so every parametrize above adds cases) — parse of the committed 365-row payloads incl. Malay month tokens, sixteen rejection paths (status, payload shape incl. empty `prayerTime`, non-year/absent `periodType`, incomplete year coverage: truncated rows / duplicate dates / dates outside `fetched_at.year`, missing marker, time format, date format: unknown month / non-string / wrong field count / unpadded day / short year / calendar-invalid, zone mismatch, ordering, non-object payload, non-object row, conflicting spellings), retry tier 1 (transient success `[2,4]`, exhaustion `[2,4,8]` ×4 attempts incl. timeout/invalid-JSON, 4xx incl. 429 fail-fast with no in-client sleep, parse-reject-no-retry), zone+status logging, golden calc vectors (6 × 8 markers ≤ tolerance, exact `imsak = fajr − 10`, unknown-method `ValueError`, provenance), scheduler (02:00 cron trigger shape with `misfire_grace_time is None` + `coalesce is True`, sync counts incl. repo-write-failure → chained retry, first-boot `ConfigError`, keep-cache-on-fail, retry chain 300/900/3600, give-up logging `after 4 attempts`, tz guard, zone/attempt logs), stale flags (fresh JAKIM false, +49h true, calc-source true); `e2e` 0 (1A-7). New invariants: no parse ever reaches the repo on failure; `fetch_year` writes nothing itself; calc never returns an unordered day; retry delays are exact constants from PRD (2/4/8s client, 300/900/3600s scheduler); a failed sync always schedules its next attempt — backend write errors and 4xx rejections alike — and no job is ever registered with a skip-on-late grace.
5. **Purity/import-linter impact:** `httpx` appears only under `adapters/` (sanctioned per `ARCHITECTURE.md:62`; scan in Task 6 proves `domain/`/`engine/`/`core/` stay clean); `adhanpy` appears only under `adapters/calc_mabims.py` (behind `CalcEngine`); no wall-clock anywhere — `fetched_at`, `run_date`, cron tz all derive from the injected `Clock`; `sqlite3` untouched; layer contract unchanged (adapters → domain → core is already allowed, `pyproject.toml:76-88`); strict pyright covers all three new adapter modules and `domain/ordering.py` with zero `type: ignore` (a library that cannot satisfy this is rejected in Task 1's criteria).
6. **Docs touched:** `PRD.md` (§6.1 pinned inventory — the row's "pinned by source research before 1A-6" clause discharges here; FR-1.1 seed wording), `ARCHITECTURE.md:43` (adapters landing status), `TESTING_STRATEGY.md:41` (negative-case line now past tense for JAKIM), new research report, `CHANGELOG.md`. `CONTEXT.md` unchanged — no glossary terms change. **No ADR** — library choice sits behind `CalcEngine` with golden-pinned outputs and both deps were pre-declared/gate-sanctioned (not hard-to-reverse); the report is the record. The 1A-2 plan's Hijri deferral (`2026-09-22-1a-2-domain-pure-logic.md:59,101`) is discharged at the *choice* level (report section); the computation has no owning slice row or contract field yet — recorded in Out of Scope as a PHASES follow-up.
7. **Rollback:** single pre-merge `git revert` — no migration exists, no contract/fixture change to unwind, no consumer of `fetch_year` outside this branch (grep-verified: only `ports.py` + `test_core_ports.py`), `pyproject.toml`/`uv.lock` revert together, the scheduler is never started by any landed code path (`create_app()` unchanged), and no device runs this code (1A-7/1A-8 wiring does not exist). Research report + `tests/data/` are inert documentation if reverted.
8. **Exit gate command output:** paste the four per-marker pytest runs, the full `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run lint-imports && uv run pytest -q`, the coverage line, all three purity `rg` scans, the `e-solat.gov.my`-in-tests scan, `uv sync --locked --all-extras`, `lint_theme` output, and the placeholder self-scan in the PR description; CI runs the same gate (`.github/workflows/ci.yml:18-34`).

## Out of Scope (later slices)

- **Boot wiring (1A-7/1A-8):** starting the scheduler in a daemon thread, choosing the production tz/`SystemClock` source, calling `run_sync` at install (the "seed fetch" in `PHASES_AND_SLICES.md:45`), boot-time `create_app()` composition, `ScheduleError`/`SyncError`→HTTP mapping, and the admin **calc-only mode** toggle (PRD §6.1 documents it; the settings surface is 1A-7).
- **FR-1.5 base Hijri computation:** Task 1 pins the library choice (the explicit 1A-2 deferral), but no slice row owns the computation or a contract field for it, and no consumer exists yet — `PHASES_AND_SLICES.md` needs a row fix (candidate: fold into 1A-7's settings surface or a 1A-6b); the payload's `hijri` rows are parsed-and-ignored until then.
- **MWL / ISNA / Egyptian methods (FR-1.3):** `calc_mabims` implements `MABIMS` only; other method strings raise `ValueError` → existing cache-miss path (`engine.py:135`).
- **Year-end rollover fetch (forward window past Dec 01):** pinned as a known endpoint limitation in report + PRD §6.1; the FR-1.2 chain covers those days until the January `period=year` fetch. Re-verify endpoint behavior (`date=` support, `period=year` availability, month tokens) whenever the report's probe date ages — the endpoint may change without notice (PRD §6.1).
- **Monthly/yearly prefetch policy, zone bulk preload, `STALE` badge UI, SSE fan-out of sync progress** — FR-1.1 mechanics beyond this row; `domain.is_stale` already implements the 48h rule and Task 6 proves it end-to-end.
